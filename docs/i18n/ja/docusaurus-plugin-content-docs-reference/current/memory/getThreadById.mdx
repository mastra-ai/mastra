---
title: "リファレンス: Memory.getThreadById() | Memory | Mastra ドキュメント"
description: "Mastra の `Memory.getThreadById()` メソッドに関するドキュメント。ID で特定のスレッドを取得します。"
---

# Memory.getThreadById()

`.getThreadById()` メソッドは、ID を指定して特定のスレッドを取得します。

## 使い方の例

```typescript
await memory?.getThreadById({ threadId: "thread-123" });
```


## パラメータ

<PropertiesTable
  content={[
    {
      name: "threadId",
      type: "string",
      description: "取得対象のスレッド ID。",
      isOptional: false,
    },
  ]}
/>

## 返り値

<PropertiesTable
  content={[
    {
      name: "thread",
      type: "Promise<StorageThreadType | null>",
      description:
        "指定されたIDに対応するスレッドを返すPromise。見つからない場合はnull。",
    },
  ]}
/>

### 関連

- [Memory クラスリファレンス](/reference/memory/memory-class)
- [Memory の概要](/docs/memory/overview)（スレッドの概念を扱います）
- [createThread](/reference/memory/createThread)
- [getThreadsByResourceId](/reference/memory/getThreadsByResourceId)