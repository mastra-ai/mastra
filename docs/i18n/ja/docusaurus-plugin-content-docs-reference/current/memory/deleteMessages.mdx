---
title: "リファレンス: Memory.deleteMessages() | Memory | Mastra ドキュメント"
description: "Mastra の `Memory.deleteMessages()` メソッドに関するドキュメント。ID を指定して複数のメッセージを削除します。"
---

<div id="memorydeletemessages">
  # Memory.deleteMessages()
</div>

`.deleteMessages()` メソッドは、複数のメッセージを ID で削除します。

<div id="usage-example">
  ## 使い方の例
</div>

```typescript copy
await memory?.deleteMessages(["671ae63f-3a91-4082-a907-fe7de78e10ec"]);
```


<div id="parameters">
  ## パラメータ
</div>

<PropertiesTable
  content={[
    {
      name: "messageIds",
      type: "string[]",
      description: "削除対象のメッセージIDの配列",
      isOptional: false,
    },
  ]}
/>

<div id="returns">
  ## 返り値
</div>

<PropertiesTable
  content={[
    {
      name: "void",
      type: "Promise<void>",
      description: "すべてのメッセージの削除完了時に解決される Promise",
    },
  ]}
/>

<div id="extended-usage-example">
  ## 応用的な使用例
</div>

```typescript title="src/test-memory.ts" showLineNumbers copy
import { mastra } from "./mastra";
import { UIMessageWithMetadata } from "@mastra/core/agent";

const agent = mastra.getAgent("agent");
const memory = await agent.getMemory();

const { uiMessages } = await memory!.query({ threadId: "thread-123" });

const messageIds = uiMessages.map(
  (message: UIMessageWithMetadata) => message.id,
);
await memory?.deleteMessages([...messageIds]);
```


<div id="related">
  ## 関連情報
</div>

- [Memory クラス リファレンス](/reference/memory/memory-class)
- [query](/reference/memory/query)
- [Memory の概要](/docs/memory/overview)