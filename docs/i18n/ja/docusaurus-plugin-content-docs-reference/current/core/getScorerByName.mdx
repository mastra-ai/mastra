---
title: "リファレンス: getScorerByName() | Core | Mastra Docs"
description: "Mastra における `getScorerByName()` メソッドのドキュメント。登録キーではなく、name プロパティに基づいてスコアラーを取得します。"
---

<div id="getscorerbyname">
  # getScorerByName()
</div>

`getScorerByName()` メソッドは、登録キーではなく `name` プロパティで検索してスコアラーを取得します。Mastra インスタンスでの登録名は分からないが表示名は分かっている場合に便利です。

<div id="usage-example">
  ## 使い方の例
</div>

```typescript
import { mastra } from "./mastra";

// name プロパティでスコアラーを取得
const relevancyScorer = mastra.getScorerByName("Answer Relevancy");

const weatherAgent = mastra.getAgent("weatherAgent");

// スコアラーを使用して AI 出力を評価
await weatherAgent.generate("ローマの天気はどうですか", {
  scorers: {
    answerRelevancy: {
      scorer: relevancyScorer,
    },
  },
});
```


<div id="parameters">
  ## パラメータ
</div>

<PropertiesTable
  content={[
    {
      name: "name",
      type: "string",
      description:
        "取得するスコアラーの name プロパティ。createScorer() でスコアラーを作成した際に指定した「name」フィールドと一致している必要があります。",
      isOptional: false,
    },
  ]}
/>

<div id="returns">
  ## 戻り値
</div>

<PropertiesTable
  content={[
    {
      name: "scorer",
      type: "MastraScorer",
      description:
        "指定した name プロパティと一致する MastraScorer インスタンス。",
    },
  ]}
/>

<div id="error-handling">
  ## エラーハンドリング
</div>

このメソッドは次の場合に `MastraError` をスローします:

* 指定した名前のスコアラーが見つからない場合
* Mastra インスタンスにスコアラーが一つも登録されていない場合

```typescript
try {
  const scorer = mastra.getScorerByName("Non-existent Scorer");
} catch (error) {
  if (error.id === "MASTRA_GET_SCORER_BY_NAME_NOT_FOUND") {
    console.log("その名前のスコアラーが見つかりません");
  }
}
```


<div id="related">
  ## 関連
</div>

- [getScorer()](../../reference/core/getScorer) - 登録キーでスコアラーを取得
- [listScorers()](../../reference/core/listScorers) - 登録済みのスコアラーをすべて取得
- [createScorer()](../../reference/scorers/create-scorer) - 名前付きスコアラーの作成方法