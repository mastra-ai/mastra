---
title: "リファレンス: Workflow.sleep() | Workflows | Mastra ドキュメント"
description: ワークフローにおける `Workflow.sleep()` メソッドのリファレンス。指定したミリ秒間、実行を一時停止します。
---

# Workflow.sleep()

`.sleep()` メソッドは、指定したミリ秒間、実行を一時停止します。固定の数値、または遅延を動的に決定するためのコールバック関数を受け取ります。

## 使い方の例

```typescript copy
workflow.sleep(5000);
```


## パラメータ

<PropertiesTable
  content={[
    {
      name: "milliseconds",
      type: "number | ((context: { inputData: any }) => number | Promise<number>)",
      description:
        "実行を停止する時間（ミリ秒）または遅延時間を返すコールバック",
      isOptional: false,
    },
  ]}
/>

## 戻り値

<PropertiesTable
  content={[
    {
      name: "workflow",
      type: "Workflow",
      description: "メソッドチェーンに使用する Workflow インスタンス",
    },
  ]}
/>

## 拡張された使用例

```typescript showLineNumbers copy
import { createWorkflow, createStep } from "@mastra/core/workflows";

const step1 = createStep({...});
const step2 = createStep({...});

export const testWorkflow = createWorkflow({...})
  .then(step1)
  .sleep(async ({ inputData }) => {
    const { delayInMs } = inputData;
    return delayInMs;
  })
  .then(step2)
  .commit();
```


## 関連情報

- [一時停止と再開](/docs/workflows/suspend-and-resume#sleep--events)