---
title: "例: チャンク配列への埋め込み | RAG | Mastra ドキュメント"
description: 類似検索のために、テキストチャンクの配列に埋め込みを生成する Mastra の使用例。
---

import GithubLink from "@site/src/components/GithubLink";


<div id="embed-chunk-array">
  # チャンク配列を埋め込む
</div>

ドキュメントをチャンク化した後、テキストチャンクを類似検索に使える数値ベクトルへ変換する必要があります。`embed` メソッドは、選択したプロバイダーとモデルを用いてテキストチャンクを埋め込み（ベクトル）に変換します。以下の例では、テキストチャンクの配列に対して埋め込みを生成する方法を示します。

```tsx copy
import { openai } from "@ai-sdk/openai";
import { MDocument } from "@mastra/rag";
import { embed } from "ai";

const doc = MDocument.fromText("テキストコンテンツ…");

const chunks = await doc.chunk();

const { embeddings } = await embedMany({
  model: openai.embedding("text-embedding-3-small"),
  values: chunks.map((chunk) => chunk.text),
});
```

<br />

<br />

<hr className="dark:border-[#404040] border-gray-300" />

<br />

<br />

<GithubLink
  link={
"https://github.com/mastra-ai/mastra/blob/main/examples/basics/rag/embed-chunk-array"
}
/>
