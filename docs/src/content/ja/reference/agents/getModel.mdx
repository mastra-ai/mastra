---
title: "リファレンス: Agent.getModel() | Agents | Mastra ドキュメント"
description: "Mastra のエージェントにおける `Agent.getModel()` メソッドのドキュメント。エージェントを駆動する言語モデルを取得します。"
---

# Agent.getModel()

`.getModel()` メソッドは、エージェントに設定された言語モデルを取得し、それが関数であれば評価して解決します。このメソッドは、エージェントの機能を支える基盤となるモデルにアクセスするために使用されます。

## 使用例

```typescript copy
await agent.getModel();
```

## パラメータ

<PropertiesTable
  content={[
    {
      name: "{ requestContext = new RequestContext() }",
      type: "{ requestContext?: RequestContext }",
      isOptional: true,
      defaultValue: "new RequestContext()",
      description: "ランタイムコンテキストを含むオプションの設定オブジェクト。",
    },
  ]}
/>

## 返り値

<PropertiesTable
  content={[
    {
      name: "model",
      type: "MastraLanguageModel | Promise<MastraLanguageModel>",
      description: "エージェントに設定された言語モデル。モデルのインスタンス、またはそのモデルに解決される Promise のいずれかです。",
    },
  ]}
/>

## 拡張された使用例

```typescript copy
await agent.getModel({
  requestContext: new RequestContext()
});
```

### オプションパラメータ

<PropertiesTable
  content={[
    {
      name: "requestContext",
      type: "RequestContext",
      isOptional: true,
      defaultValue: "undefined",
      description: "依存性注入とコンテキスト情報のためのランタイムコンテキスト。",
    },
  ]}
/>

## 関連項目

- [エージェントの概要](../../docs/agents/overview.mdx)
- [エージェントのランタイムコンテキスト](../../docs/agents/runtime-context.mdx)