---
title: "リファレンス: Agent.getModel() | Agents | Mastra ドキュメント"
description: "Mastra のエージェントで使用される `Agent.getModel()` メソッドのドキュメント。エージェントを動かす言語モデルを取得します。"
---

# Agent.getModel()

`.getModel()` メソッドは、エージェントに設定された言語モデルを取得し、それが関数である場合は実体を解決します。このメソッドは、エージェントの機能を支える基盤となるモデルへアクセスするために使用されます。

## 使い方の例

```typescript copy
await agent.getModel();
```


## パラメータ

<PropertiesTable
  content={[
    {
      name: "{ runtimeContext = new RuntimeContext() }",
      type: "{ runtimeContext?: RuntimeContext }",
      isOptional: true,
      defaultValue: "new RuntimeContext()",
      description: "ランタイムコンテキストを含む任意指定の設定オブジェクト。",
    },
  ]}
/>

## 返り値

<PropertiesTable
  content={[
    {
      name: "model",
      type: "MastraLanguageModel | Promise<MastraLanguageModel>",
      description: "エージェントに設定された言語モデル。モデルの直接インスタンス、またはそのモデルに解決される Promise のいずれかです。",
    },
  ]}
/>

## 詳細な使用例

```typescript copy
await agent.getModel({
  runtimeContext: new RuntimeContext()
});
```


### オプションのパラメーター

<PropertiesTable
  content={[
    {
      name: "runtimeContext",
      type: "RuntimeContext",
      isOptional: true,
      defaultValue: "undefined",
      description: "依存性注入やコンテキスト情報のためのランタイムコンテキスト。",
    },
  ]}
/>

## 関連項目

- [Agents 概要](../../docs/agents/overview.mdx)
- [ランタイムコンテキスト](../../docs/server-db/runtime-context.mdx)