---
title: "リファレンス: Agent.getAgent() | Agents | Mastra Docs"
description: "Mastra の `Agent.getAgent()` メソッドのドキュメント。エージェントを名前で取得します。"
---

# Mastra.getAgent()

`.getAgent()` メソッドは、エージェントを取得するために使用します。このメソッドは、エージェント名を表す `string` 型のパラメーターを1つ受け取ります。

## 使い方の例

```typescript copy
mastra.getAgent("testAgent");
```


## パラメータ

<PropertiesTable
  content={[
    {
      name: "name",
      type: "TAgentName extends keyof TAgents",
      description: "取得するエージェントの名前。Mastra の設定に存在する有効なエージェント名である必要があります。",
    },
  ]}
/>

## 戻り値

<PropertiesTable
  content={[
    {
      name: "agent",
      type: "TAgents[TAgentName]",
      description: "指定された名前のエージェントのインスタンス。エージェントが見つからない場合はエラーを送出します。",
    },
  ]}
/>

## 関連

- [エージェントの概要](../../docs/agents/overview.mdx)
- [ダイナミックエージェント](../../docs/agents/dynamic-agents.mdx)