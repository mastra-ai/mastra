---
title: "Netlify Deployer"
description: "Mastra アプリケーションを Netlify Functions にデプロイする NetlifyDeployer クラスのドキュメント。"
---

# NetlifyDeployer

`NetlifyDeployer` クラスは、スタンドアロンの Mastra アプリケーションを Netlify にデプロイします。設定やデプロイの管理を行い、Netlify 固有の機能を追加してベースの [Deployer](/reference/deployer/deployer) クラスを拡張します。

## 使い方の例

```typescript filename="src/mastra/index.ts" showLineNumbers copy
import { Mastra } from "@mastra/core/mastra";
import { NetlifyDeployer } from "@mastra/deployer-netlify";

export const mastra = new Mastra({
  // 省略
  deployer: new NetlifyDeployer()
});
```
