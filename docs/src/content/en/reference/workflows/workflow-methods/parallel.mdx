---
title: "Reference: Workflow.parallel() | Workflows"
description: Documentation for the `Workflow.parallel()` method in workflows, which executes multiple steps in parallel.
packages:
  - "@mastra/core"
---

# Workflow\.parallel()

The `.parallel()` method executes multiple steps in parallel.

## Usage example

```typescript
workflow.parallel([step1, step2])

// With allowFailure
workflow.parallel([step1, step2], { allowFailure: true })
```

## Parameters

<PropertiesTable
  content={[
  {
    name: 'steps',
    type: 'Step[]',
    description: 'The step instances to execute in parallel',
    isOptional: false,
  },
  {
    name: 'opts',
    type: '{ allowFailure?: boolean }',
    description:
      'Optional configuration. Set `allowFailure: true` to allow the parallel block to succeed even when individual steps fail. Failed step outputs are `null` in the downstream step input. Error details remain available in `result.steps`.',
    isOptional: true,
  },
]}
/>

## Returns

<PropertiesTable
  content={[
  {
    name: 'workflow',
    type: 'Workflow',
    description: 'The workflow instance for method chaining',
  },
]}
/>

## Related

- [Simultaneous steps with parallel](/docs/workflows/control-flow#simultaneous-steps-with-parallel)
