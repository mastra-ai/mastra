---
title: "Reference: Workflow.commit() | Workflows"
description: "Use Workflow.commit() to finalize a workflow definition, validate its execution graph, and return the committed workflow ready to create runs."
packages:
  - "@mastra/core"
---

# Workflow\.commit()

The `.commit()` method finalizes the workflow and returns the final result.

## Usage example

```typescript
workflow.then(step1).commit()
```

## Returns

<PropertiesTable
  content={[
  {
    name: 'workflow',
    type: 'Workflow',
    description: 'The workflow instance for method chaining',
  },
]}
/>

## Related

- [Control Flow](/docs/workflows/control-flow)
