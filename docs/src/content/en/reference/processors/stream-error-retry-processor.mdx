---
title: "Reference: StreamErrorRetryProcessor | Processors"
description: "API reference for the StreamErrorRetryProcessor processor in Mastra, which retries transient stream errors."
packages:
  - "@mastra/core"
---

# StreamErrorRetryProcessor

`StreamErrorRetryProcessor` is an **error processor** that retries transient errors emitted after an LLM stream starts. Use it with provider-specific matchers for stream error shapes that the AI SDK's request-level retry does not handle.

The processor isn't enabled by default in core. Add it to `errorProcessors` for agents that need stream-error retry handling.

## Usage example

Add `StreamErrorRetryProcessor` to `errorProcessors` with a matcher for OpenAI Responses stream errors:

```typescript title="src/mastra/agents/openai-agent.ts"
import { Agent } from '@mastra/core/agent'
import {
  StreamErrorRetryProcessor,
  isRetryableOpenAIResponsesStreamError,
} from '@mastra/core/processors'

export const agent = new Agent({
  name: 'openai-agent',
  instructions: 'You are a helpful assistant.',
  model: 'openai/gpt-5',
  errorProcessors: [
    new StreamErrorRetryProcessor({ matchers: [isRetryableOpenAIResponsesStreamError] }),
  ],
})
```

## How it works

The processor checks the error and its cause chain for:

- Provider retry metadata: `isRetryable === true`
- Matcher results: Any configured matcher that returns `true`

When the error is retryable, the processor returns `{ retry: true }`. It doesn't mutate messages.

## OpenAI Responses matcher

`isRetryableOpenAIResponsesStreamError` matches OpenAI Responses stream error chunks with `type: 'error'` or `type: 'response.failed'`. It retries known transient OpenAI error codes and, as a fallback, errors with explicit retry guidance such as `You can retry your request`.

## Constructor parameters

<PropertiesTable
  content={[
  {
    name: 'options',
    type: 'StreamErrorRetryProcessorOptions',
    description: 'Configuration for retry handling.',
    isOptional: true,
    properties: [
      {
        name: 'maxRetries',
        type: 'number',
        description: 'Maximum number of retries requested by this processor.',
        isOptional: true,
        defaultValue: '1',
      },
      {
        name: 'matchers',
        type: 'StreamErrorRetryMatcher[]',
        description: 'Functions that identify provider-specific retryable stream errors.',
        isOptional: true,
        defaultValue: '[]',
      },
    ],
  },
]}
/>

## Properties

<PropertiesTable
  content={[
  {
    name: 'id',
    type: "'stream-error-retry-processor'",
    description: 'Processor identifier.',
    isOptional: false,
  },
  {
    name: 'name',
    type: "'Stream Error Retry Processor'",
    description: 'Processor display name.',
    isOptional: false,
  },
  {
    name: 'processAPIError',
    type: '(args: ProcessAPIErrorArgs) => ProcessAPIErrorResult | void',
    description: 'Retries stream errors up to the configured retry limit.',
    isOptional: false,
  },
]}
/>

## Related

- [Processor interface](/reference/processors/processor-interface)
- [Processors](/docs/agents/processors)
