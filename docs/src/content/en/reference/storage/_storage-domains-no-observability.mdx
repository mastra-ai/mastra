This storage adapter supports the following domains:

- **`memory`** - Threads, messages, and resources
- **`evals`** - Score storage and evaluation data
- **`workflows`** - Workflow run snapshots and state

**Note:** The `observability` domain is not currently supported. Use another storage adapter for observability features.

