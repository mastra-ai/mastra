---
title: "Reference: DatasetsManager.getExperiment() | Datasets"
description: "Documentation for the `DatasetsManager.getExperiment()` method, which retrieves a specific experiment by ID across all datasets."
packages:
  - "@mastra/core"
---

# DatasetsManager.getExperiment()

Retrieves a specific experiment (run) by ID. Unlike `dataset.getExperiment()`, this works across all datasets without needing a dataset reference first.

## Usage example

```typescript
import { Mastra } from "@mastra/core";

const mastra = new Mastra({ /* storage config */ });

// Get experiment directly without knowing the dataset
const experiment = await mastra.datasets.getExperiment({
  experimentId: "exp-id",
});

console.log(`Dataset: ${experiment.datasetId}`);
console.log(`Status: ${experiment.status}`);
```

## Parameters

<PropertiesTable
  content={[
    {
      name: "experimentId",
      type: "string",
      description: "ID of the experiment to retrieve.",
    },
  ]}
/>

## Returns

<PropertiesTable
  content={[
    {
      name: "result",
      type: "Promise<Experiment>",
      description: "The experiment record. See dataset.listExperiments() for the full shape.",
    },
  ]}
/>
