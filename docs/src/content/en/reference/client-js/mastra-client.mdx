---
title: MastraClient
description: Learn how to interact with Mastra using the client-js SDK.
---

# MastraClient Class

The Mastra Client SDK provides a simple and type-safe interface for interacting with your [Mastra Server](/docs/deployment/server-deployment.mdx) from your client environment.

## Usage example

```typescript filename="lib/mastra/mastra-client.ts" showLineNumbers copy
import { MastraClient } from "@mastra/client-js";

export const mastraClient = new MastraClient({
  baseUrl: "http://localhost:4111/",
});
```

## Constructor parameters

<PropertiesTable
  content={[
    {
      name: "baseUrl",
      type: "string",
      description: "The base URL for the Mastra API. All requests will be sent relative to this URL.",
      isOptional: false,
    },
    {
      name: "retries",
      type: "number",
      description: "The number of times a request will be retried on failure before throwing an error.",
      isOptional: true,
      defaultValue: "3",
    },
    {
      name: "backoffMs",
      type: "number",
      description: "The initial delay in milliseconds before retrying a failed request. This value is doubled with each retry (exponential backoff).",
      isOptional: true,
      defaultValue: "300",
    },
    {
      name: "maxBackoffMs",
      type: "number",
      description: "The maximum backoff time in milliseconds. Prevents retries from waiting too long between attempts.",
      isOptional: true,
      defaultValue: "5000",
    },
    {
      name: "headers",
      type: "Record<string, string>",
      description: "An object containing custom HTTP headers to include with every request.",
      isOptional: true,
    },
    {
      name: "abortSignal",
      type: "AbortSignal",
      description: "Abort signal for request cancellation.",
      isOptional: true,
    },
    {
      name: "credentials",
      type: '"omit" | "same-origin" | "include"',
      description: "Credentials mode for requests. See https://developer.mozilla.org/en-US/docs/Web/API/Request/credentials for more info.",
      isOptional: true,
    },
  ]}
/>

## Methods

<PropertiesTable
  content={[
    {
      name: "getAgents()",
      type: "Promise<Record<string, GetAgentResponse>>",
      description: "Returns all available agent instances.",
    },
    {
      name: "getAgent(agentId)",
      type: "Agent",
      description: "Retrieves a specific agent instance by ID.",
    },
    {
      name: "getMemoryThreads(params)",
      type: "Promise<StorageThreadType[]>",
      description: "Retrieves memory threads for the specified resource and agent. Requires a `resourceId` and an `agentId`.",
    },
    {
      name: "createMemoryThread(params)",
      type: "Promise<MemoryThread>",
      description: "Creates a new memory thread with the given parameters.",
    },
    {
      name: "getMemoryThread(threadId)",
      type: "Promise<MemoryThread>",
      description: "Fetches a specific memory thread by ID.",
    },
    {
      name: "saveMessageToMemory(params)",
      type: "Promise<void>",
      description: "Saves one or more messages to the memory system.",
    },
    {
      name: "getMemoryStatus(agentId)",
      type: "Promise<{ result: boolean }>",
      description: "Returns the current status of the memory system for a specific agent.",
    },
    {
      name: "getNetworkMemoryThreads(params)",
      type: "Promise<StorageThreadType[]>",
      description: "Retrieves memory threads for network-based agents.",
    },
    {
      name: "createNetworkMemoryThread(params)",
      type: "Promise<MemoryThread>",
      description: "Creates a new memory thread for network-based agents.",
    },
    {
      name: "getNetworkMemoryThread(threadId, networkId)",
      type: "Promise<MemoryThread>",
      description: "Fetches a specific network memory thread by ID.",
    },
    {
      name: "saveNetworkMessageToMemory(params)",
      type: "Promise<void>",
      description: "Saves messages to network-based memory system.",
    },
    {
      name: "getNetworkMemoryStatus(networkId)",
      type: "Promise<{ result: boolean }>",
      description: "Returns the current status of the network memory system.",
    },
    {
      name: "getTools()",
      type: "Promise<Record<string, GetToolResponse>>",
      description: "Returns all available tools.",
    },
    {
      name: "getTool(toolId)",
      type: "Tool",
      description: "Retrieves a specific tool instance by ID.",
    },
    {
      name: "getWorkflows()",
      type: "Promise<Record<string, GetWorkflowResponse>>",
      description: "Returns all available workflow instances.",
    },
    {
      name: "getWorkflow(workflowId)",
      type: "Workflow",
      description: "Retrieves a specific workflow instance by ID.",
    },
    {
      name: "getVector(vectorName)",
      type: "Vector",
      description: "Returns a vector store instance by name.",
    },
    {
      name: "getLogs(params)",
      type: "Promise<LogEntry[]>",
      description: "Fetches system logs matching the provided filters.",
    },
    {
      name: "getLog(params)",
      type: "Promise<LogEntry>",
      description: "Retrieves a specific log entry by ID or filter.",
    },
    {
      name: "getLogTransports()",
      type: "Promise<{ transports: string[] }>",
      description: "Returns the list of configured log transport types.",
    },
    {
      name: "getTelemetry(params)",
      type: "Promise<GetTelemetryResponse>",
      description: "Fetches telemetry data with optional filtering parameters.",
    },
    {
      name: "getNetworks()",
      type: "Promise<Array<GetNetworkResponse>>",
      description: "Returns all available network instances.",
    },
    {
      name: "getVNextNetworks()",
      type: "Promise<Array<GetVNextNetworkResponse>>",
      description: "Returns all available vNext network instances.",
    },
    {
      name: "getNetwork(networkId)",
      type: "Network",
      description: "Retrieves a specific network instance by ID.",
    },
    {
      name: "getVNextNetwork(networkId)",
      type: "VNextNetwork",
      description: "Retrieves a specific vNext network instance by ID.",
    },
    {
      name: "getMcpServers(params)",
      type: "Promise<McpServerListResponse>",
      description: "Returns all available MCP servers with optional pagination.",
    },
    {
      name: "getMcpServerDetails(serverId, params)",
      type: "Promise<ServerDetailInfo>",
      description: "Retrieves detailed information about a specific MCP server.",
    },
    {
      name: "getMcpServerTools(serverId)",
      type: "Promise<McpServerToolListResponse>",
      description: "Returns all tools available on a specific MCP server.",
    },
    {
      name: "getMcpServerTool(serverId, toolId)",
      type: "MCPTool",
      description: "Retrieves a specific tool from an MCP server.",
    },
    {
      name: "getA2A(agentId)",
      type: "A2A",
      description: "Returns an A2A (Agent-to-Agent) instance for the specified agent.",
    },
    {
      name: "getWorkingMemory({agentId, threadId, resourceId})",
      type: "Promise<any>",
      description: "Retrieves working memory for a specific thread and agent.",
    },
    {
      name: "updateWorkingMemory({agentId, threadId, workingMemory, resourceId})",
      type: "Promise<any>",
      description: "Updates working memory for a specific thread and agent.",
    },
    {
      name: "getScorers()",
      type: "Promise<Record<string, GetScorerResponse>>",
      description: "Returns all available scorers.",
    },
    {
      name: "getScorer(scorerId)",
      type: "Promise<GetScorerResponse>",
      description: "Retrieves a specific scorer by ID.",
    },
    {
      name: "getScoresByScorerId(params)",
      type: "Promise<GetScoresResponse>",
      description: "Fetches scores filtered by scorer ID.",
    },
    {
      name: "getScoresByRunId(params)",
      type: "Promise<GetScoresResponse>",
      description: "Fetches scores filtered by run ID.",
    },
    {
      name: "getScoresByEntityId(params)",
      type: "Promise<GetScoresResponse>",
      description: "Fetches scores filtered by entity ID.",
    },
    {
      name: "saveScore(params)",
      type: "Promise<SaveScoreResponse>",
      description: "Saves a new score entry.",
    },
    {
      name: "getLegacyWorkflows()",
      type: "Promise<Record<string, GetLegacyWorkflowResponse>>",
      description: "Returns all available legacy workflow instances.",
    },
    {
      name: "getLegacyWorkflow(workflowId)",
      type: "LegacyWorkflow",
      description: "Retrieves a specific legacy workflow instance by ID.",
    },
    {
      name: "getAgentBuilderActions()",
      type: "Promise<Record<string, WorkflowInfo>>",
      description: "Returns all available agent builder actions.",
    },
    {
      name: "getAgentBuilderAction(actionId)",
      type: "Workflow",
      description: "Retrieves a specific agent builder action by ID.",
    },
  ]}
/>

