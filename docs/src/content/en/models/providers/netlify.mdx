---
title: "Netlify | Models | Mastra"
description: "Use Netlify models with Mastra. 32 models available."
---

import { ProviderModelsTable } from "@/components/provider-models-table";


# <img src="/logos/netlify.svg" alt="Netlify logo" className="inline w-8 h-8 mr-2 align-middle" />Netlify

Access 32 Netlify models through Mastra's model router. Authentication is handled automatically using the `NETLIFY_TOKEN,NETLIFY_SITE_ID` environment variable.

Learn more in the [Netlify documentation](https://docs.netlify.com/build/ai-gateway/overview/).

```bash
NETLIFY_TOKEN,NETLIFY_SITE_ID=your-api-key
```

```typescript
import { Agent } from "@mastra/core";

const agent = new Agent({
  name: "my-agent",
  instructions: "You are a helpful assistant",
  model: "netlify/anthropic/claude-3-5-haiku-20241022"
});

// Generate a response
const response = await agent.generate("Hello!");

// Stream a response
const stream = await agent.stream("Tell me a story");
for await (const chunk of stream) {
  console.log(chunk);
}
```

## Models

<ProviderModelsTable 
  models={[]}
/>

## Advanced Configuration

### Custom Headers

```typescript
const agent = new Agent({
  name: "custom-agent",
  model: {
    url: "undefined",
    modelId: "anthropic/claude-3-5-haiku-20241022",
    apiKey: process.env.NETLIFY_TOKEN,NETLIFY_SITE_ID,
    headers: {
      "X-Custom-Header": "value"
    }
  }
});
```

### Dynamic Model Selection

```typescript
const agent = new Agent({
  name: "dynamic-agent",
  model: ({ runtimeContext }) => {
    const useAdvanced = runtimeContext.task === "complex";
    return useAdvanced 
      ? "netlify/anthropic/claude-3-5-haiku-20241022"
      : "netlify/anthropic/claude-3-5-haiku-latest";
  }
});
```
