---
title: "Worker reference | Deployment"
description: "Environment variables, CLI commands, and configuration options for Mastra workers."
packages:
  - "@mastra/core"
---

# Worker reference

Worker reference content has moved to dedicated reference pages:

- **Environment variables, worker types, and storage backends** — [Workers reference](/reference/workers/overview)
- **CLI commands** (`mastra worker build`, `mastra worker start`) — [CLI reference](/reference/cli/mastra#mastra-worker-build)
- **Constructor parameters** (`workers`, `backgroundTasks`, `scheduler`) — [Mastra class reference](/reference/core/mastra-class#constructor-parameters)
