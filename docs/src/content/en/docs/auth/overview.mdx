---
title: "Auth"
description: "Choose and configure authentication for Mastra APIs and Studio using JWT, built-in identity providers, third-party integrations, or custom auth providers."
packages:
  - "@mastra/auth"
---

import { IntegrationGrid } from '@site/src/components/integrations/grid';
import { CardGrid, CardGridItem } from "@site/src/components/cards/card-grid";

# Auth

Mastra lets you choose how you handle authentication, so you can secure access to your API and [Studio](/docs/studio/overview) using the identity system that fits your stack.

You can start with basic shared secret JWT authentication and switch to known providers when you need more advanced identity features.

## What auth secures

Choose where to configure authentication:

- **`server.auth`**: Authenticates API requests. Studio also uses this provider unless you configure `studio.auth` separately.
- **`studio.auth`**: Authenticates Studio requests with a separate provider. It doesn't require ordinary API callers to use that provider. If `server.auth` is unset, ordinary API requests remain unauthenticated.

Studio displays the login options supported by its provider. Configure [role-based access control](/docs/studio/auth#role-based-access-control) separately to restrict what each team member can do.

A Studio login screen doesn't secure an otherwise public API. See [Separate Studio and API authentication](/docs/studio/auth#separate-studio-and-api-authentication) for the routing behavior and security implications.

Authentication is optional. If no auth is configured, all routes and Studio are publicly accessible. Public access can be enabled on individual custom routes using `requiresAuth: false`.

See [Custom API Routes](/docs/server/custom-api-routes#authentication) for controlling authentication on custom endpoints. Visit the [Studio Auth docs](/docs/studio/auth) for more on securing your Studio deployment.

:::warning
The default auth configuration protects `/api/*` and treats `/api`, `/api/auth/*` as public. When you set a custom `server.apiPrefix`, those defaults no longer match and built-in routes fall outside the protected pattern. Update `server.auth.protected` and `server.auth.public` to reference your new prefix.
:::

:::note

Authentication for Studio is supported by Simple Auth, JWT, WorkOS, Better Auth, Google, and Clerk. Available login methods and required configuration depend on the provider. See the provider's guide for Studio single sign-on (SSO) setup, which can require credentials beyond those used for API token verification.

:::

## Available providers

### Built-in

<CardGrid>
  <CardGridItem
    title="Simple Auth"
    href="/docs/auth/simple-auth"
    description="Token-to-user mapping for development and API keys"
  />

  <CardGridItem title="JSON Web Token" href="/docs/auth/jwt" description="HMAC-signed JWT verification" />
</CardGrid>

### Third-party integrations

<IntegrationGrid section="Auth" />

### Advanced

<CardGrid>
  <CardGridItem
    title="Composite Auth"
    href="/docs/auth/composite-auth"
    description="Combine multiple auth providers"
  />

  <CardGridItem
    title="Custom Provider"
    href="/docs/auth/custom-auth-provider"
    description="Build your own provider"
  />
</CardGrid>
