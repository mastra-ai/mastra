---
title: "A2A overview | A2A"
description: "Learn how Mastra uses A2A for remote agents, subagents, and Mastra Client integrations."
packages:
  - "@mastra/client-js"
  - "@mastra/core"
  - "@mastra/server"
---

# A2A overview

Mastra exposes agents through the Agent-to-Agent (A2A) protocol so another agent or service can discover them, send work to them, and track task progress over time. Use A2A when you need one Mastra agent to act as a remote agent for another system.

## When to use A2A

- You need a standard agent card and execution endpoint for another service
- You want one Mastra agent to call another agent across a network boundary
- You need task-oriented streaming, resumable tasks, or callback delivery

## Pages in this section

- [Build remote A2A agents](/docs/a2a/build-remote-agents): Expose a Mastra agent over A2A from Mastra Server
- [Use remote A2A agents as subagents](/docs/a2a/remote-agents-as-subagents): Compose remote agents into a larger multi-agent system
- [Connect to a remote A2A agent with Mastra Client](/docs/a2a/connect-with-mastra-client): Discover agent cards, send messages, and manage remote tasks from a client

## How A2A works in Mastra

In Mastra, A2A usually involves two roles:

- **Client agent**: The caller that discovers an agent card, sends a message, and listens for task updates
- **Remote agent**: The Mastra agent running behind the A2A endpoint

The remote agent is still a normal Mastra agent. It keeps using its own instructions, tools, memory, workflows, and server configuration. A2A changes how that agent is reached, not how the agent is authored.

The flow looks like this:

1. Register a standard Mastra agent in your `Mastra` instance.
1. Start Mastra Server or mount Mastra routes into your own server.
1. Mastra exposes that agent through an agent card at `/.well-known/:agentId/agent-card.json` and an execution endpoint at `/a2a/:agentId`.
1. A client agent or application reads the card, sends messages, and receives task, artifact, and status events.

This makes A2A useful for agent-to-agent calls across process boundaries, across services, or across teams. The client does not need direct access to the remote agent's internal code or tools. It only needs the A2A endpoint.

## Get started

Start with the page that matches your role:

- If you are exposing a Mastra agent for others to call, read [Build remote A2A agents](/docs/a2a/build-remote-agents)
- If you are calling a remote A2A agent from an app or service, read [Connect to a remote A2A agent with Mastra Client](/docs/a2a/connect-with-mastra-client)

## Next steps

- [Build remote A2A agents](/docs/a2a/build-remote-agents)
- [Use remote A2A agents as subagents](/docs/a2a/remote-agents-as-subagents)
- [Connect to a remote A2A agent with Mastra Client](/docs/a2a/connect-with-mastra-client)
