---
title: 'Knowledge capture'
description: 'Capture session knowledge at the resource boundary without widening authority.'
packages:
  - '@mastra/core'
---

# Knowledge capture

Capture converts a session's useful facts into Knowledge at the resource boundary. It's separate from authorization.

## Choose capture scopes

The host derives capture scopes from the authenticated resource, thread, and application context. Keep resource and thread scopes when clamping the operation. Dropping them can hide valid shared content or leak organization-wide content.

Captured records write to an uncurated companion associated with the curated destination. A companion is an ordinary node with `role: "mirror"` and the same governed read and write paths as other nodes. Treat its content as provisional until curation integrates it.

## Resolve mentions inside the frontier

Resolve wikilinks only among uniquely visible targets in the session-clamped scopes. Captured text can't add authority. If a referenced target is hidden or ambiguous, preserve privacy and leave it unresolved.

## Separate capture from promotion

Capture appends evidence without promoting facts into curated nodes. It leaves grants and structure unchanged. Promotion is a curation action with its own authorization and compare-and-swap checks.

## Protect semantic indexing

Before vector upsert, hydrate the node or record through authorized Knowledge reads and sanitize hidden `scope_ids`, names, and text from metadata. Recheck visibility when applying an outbox entry because permissions may have changed since capture.

## Test the real boundary

Cover principals with overlapping and private resource scopes, including:

- Hidden mention targets producing no edge or text leak.
- Delete and restore draining semantic outbox work.
- Restart continuity for captured records.
- Revocation after capture but before semantic indexing.

Use the installed memory package's capture configuration and TypeScript types for exact processor wiring.
