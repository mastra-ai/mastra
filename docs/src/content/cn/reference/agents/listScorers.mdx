---
title: "参考文档：Agent.listScorers() | 智能体"
description: "Mastra 智能体中 `Agent.listScorers()` 方法的文档，用于检索评分配置。"
packages:
  - "@mastra/core"
---

# Agent.listScorers()

`.listScorers()` 方法检索为智能体配置的评分配置，如果它是函数则解析它。此方法提供对用于评估智能体响应和性能的评分系统的访问。

## 使用示例

```typescript
await agent.listScorers();
```

## 参数

<PropertiesTable
  content={[
    {
      name: "options",
      type: "{ requestContext?: RequestContext }",
      isOptional: true,
      defaultValue: "{}",
      description: "包含请求上下文的选择性配置对象。",
    },
  ]}
/>

## 返回值

<PropertiesTable
  content={[
    {
      name: "scorers",
      type: "MastraScorers | Promise<MastraScorers>",
      description:
        "为智能体配置的评分配置，可以是直接对象，也可以是解析为评分器的 promise。",
    },
  ]}
/>

## 扩展使用示例

```typescript
await agent.listScorers({
  requestContext: new RequestContext(),
});
```

### 选项参数

<PropertiesTable
  content={[
    {
      name: "requestContext",
      type: "RequestContext",
      isOptional: true,
      defaultValue: "new RequestContext()",
      description:
        "用于依赖注入和上下文信息的请求上下文。",
    },
  ]}
/>

## 相关内容

- [智能体概述](/docs/cn/docs/agents/overview)
- [请求上下文](/docs/cn/docs/server/request-context)
