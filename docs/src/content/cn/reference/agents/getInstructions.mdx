---
title: "参考文档：Agent.getInstructions() | 智能体"
description: "Mastra 智能体中 `Agent.getInstructions()` 方法的文档，用于检索指导智能体行为的指令。"
packages:
  - "@mastra/core"
---

# Agent.getInstructions()

`.getInstructions()` 方法检索为智能体配置的指令，如果它们是函数则解析它们。这些指令指导智能体的行为并定义其能力和约束。

## 使用示例

```typescript
await agent.getInstructions();
```

## 参数

<PropertiesTable
  content={[
    {
      name: "options",
      type: "{ requestContext?: RequestContext }",
      isOptional: true,
      defaultValue: "{}",
      description: "包含请求上下文的选择性配置对象。",
    },
  ]}
/>

## 返回值

<PropertiesTable
  content={[
    {
      name: "instructions",
      type: "SystemMessage | Promise<SystemMessage>",
      description:
        "为智能体配置的指令。SystemMessage 可以是：string | string[] | CoreSystemMessage | CoreSystemMessage[] | SystemModelMessage | SystemModelMessage[]。直接返回或作为解析为指令的 promise 返回。",
    },
  ]}
/>

## 扩展使用示例

```typescript
await agent.getInstructions({
  requestContext: new RequestContext(),
});
```

### 选项参数

<PropertiesTable
  content={[
    {
      name: "requestContext",
      type: "RequestContext",
      isOptional: true,
      defaultValue: "undefined",
      description:
        "用于依赖注入和上下文信息的请求上下文。",
    },
  ]}
/>

## 相关内容

- [智能体概述](/docs/cn/docs/agents/overview)
- [请求上下文](/docs/cn/docs/server/request-context)
