/**
 * Stub for the Kapa theme's `@theme/Chat`. Rendered only when the Kapa theme
 * is not registered (missing credentials, e.g. CI) — the local
 * `DocRoot/Layout/Main` override hides the chat UI in that case, so this
 * component never actually appears.
 */
export default function Chat(): null {
  return null
}
