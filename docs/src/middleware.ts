export { middleware } from "nextra/locales";

export const config = {
  // Matcher for specific paths: /docs, /examples, /showcase,  /guides, /reference and /models
  matcher: ["/(docs|examples|showcase|guides|reference|models)/:path*"],
};
