export { RequestContext, MASTRA_RESOURCE_ID_KEY, MASTRA_THREAD_ID_KEY } from '../request-context';

// Backwards compatibility alias - RuntimeContext was renamed to RequestContext
export { RequestContext as RuntimeContext } from '../request-context';
