import { describe, it } from 'vitest';

// All real-model tests have been moved to aisdk-voice.e2e.test.ts
// because they all depend on real OpenAI models (@ai-sdk/openai-v5).
describe('AisdkVoice', () => {
  it.skip('tests moved to aisdk-voice.e2e.test.ts', () => {});
});
