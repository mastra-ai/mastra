{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "tasks": {
    "build": {
      "inputs": ["$TURBO_DEFAULT$", "public/**/*"],
      "dependsOn": ["^build"],
      "outputs": ["dist/**"]
    }
  }
}
