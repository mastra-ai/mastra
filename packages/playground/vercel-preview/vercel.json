{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "installCommand": "pnpm install --frozen-lockfile --filter studio-preview...",
  "buildCommand": "MASTRA_AGENT_SIGNALS=false pnpm turbo build:vercel --filter studio-preview --concurrency=2"
}
