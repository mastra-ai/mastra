// Mirrors the `--radius-*` tokens in theme.css for consumers that cannot read
// CSS, and gives `lib/tw-merge-config.ts` its border-radius conflict group.
export const BorderRadius = {
  none: '0px',
  sm: '4px',
  md: '6px',
  lg: '10px',
  xl: '14px',
};
