'use client';

// Placeholder for Task 017
export function VersionCompareDialog({ agentId, open, onOpenChange }: { agentId: string; open: boolean; onOpenChange: (open: boolean) => void }) {
  return null;
}
