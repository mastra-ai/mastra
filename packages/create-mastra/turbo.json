{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "tasks": {
    "build": {
      "dependsOn": [
        "^build",
        "@mastra/core#build",
        "@mastra/libsql#build",
        "@mastra/observability#build",
        "@mastra/memory#build",
        "@mastra/loggers#build",
        "@mastra/evals#build"
      ],
      "outputs": ["dist/**"]
    }
  }
}
