{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "tasks": {
    "test": {
      "dependsOn": ["^build", "@mastra/core#pre-build", "@mastra/core#build"],
      "inputs": ["src/**/*.ts", "test/**/*.ts", "package.json"]
    }
  }
}
