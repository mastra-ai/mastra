# Gateway — single key proxies to OpenAI, Anthropic, Google, etc.
# https://mastra.ai/models/gateways/mastra
MASTRA_GATEWAY_API_KEY=

# Optional: override the model. Defaults to mastra/openai/gpt-5-mini through the
# Gateway. Any Gateway model works, and you can point straight at a provider
# instead — e.g. LLM_MODEL=openai/gpt-5-mini with OPENAI_API_KEY set, or
# LLM_MODEL=anthropic/claude-sonnet-4-6 with ANTHROPIC_API_KEY. This is the
# escape hatch when the Gateway account is out of inference credits.
# LLM_MODEL=

# Optional: how long an idempotent `circle` read may run before it is treated as
# hung and retried. Defaults to 120000 (2 min). Payments are never killed on a
# timer, however long they take.
CIRCLE_CLI_TIMEOUT_MS=

# Circle needs no API key. The Circle CLI holds the session, and you log in
# yourself in your own terminal:
#
#   npm install -g @circle-fin/cli
#   circle wallet login
#   circle wallet status
#
# Accept Circle's Terms of Use at that prompt. An agent must never accept them
# on your behalf, so this template will not do it for you.
# https://developers.circle.com/agent-stack
