# @mastra/connect is on the alpha train, whose prerelease versions don't
# satisfy stable-style peer ranges under npm's strict semver rules.
# Remove this once @mastra/connect ships a stable release.
legacy-peer-deps=true
