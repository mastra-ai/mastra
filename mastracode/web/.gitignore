# Vite SPA build output — copied into .mastra/output/ by `mastra build`
# because it lives under the Mastra entry's public/ dir.
src/mastra/public/factory/

# Factory skill assets — generated from @mastra/factory by
# scripts/sync-factory-skills.mjs during prebuild. Never edit or commit;
# edit mastracode/factory/factory-skills instead.
src/mastra/public/factory-skills/
