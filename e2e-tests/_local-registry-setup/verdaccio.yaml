storage: ./storage

uplinks:
  npmjs:
    url: https://registry.npmjs.org/

packages:
  'mastra':
    access: $all
    publish: $all
    unpublish: $all

  '@mastra/*':
    access: $all
    publish: $all
    unpublish: $all

  'create-mastra':
    access: $all
    publish: $all
    unpublish: $all

  '**':
    access: $all
    publish: $all
    unpublish: $all
    proxy: npmjs

web:
  enable: true

auth:
  auth-memory:
    users:
      admin:
        name: mastra
        password: mastra-ai

log:
  - { type: stdout, format: pretty, level: error }
