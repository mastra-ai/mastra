---
'@mastra/mcp': minor
---

Added tracing support to MCP tool execution. Tools now accept an optional `tracingContext` parameter that enables observability tracking for MCP tool calls. When provided, a span of type `MCP_TOOL_CALL` is created with metadata including server name and version. The span captures input data, tracks success/failure, and properly handles errors and reconnection flows.
