---
'@mastra/playground-ui': patch
---

Fixed the search box in the TabbedContainer tab rail so it renders as a standard search field. The filter button now sits next to it as its own control instead of inside the search box.
