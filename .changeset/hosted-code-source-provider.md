---
'@mastra/deployer': minor
'mastra': minor
---

Added hosted source-provider context to Studio deployments.

Hosted Studio builds can now receive the Platform API endpoint and project ID through injected HTML config. Studio uses the server-reported source capabilities to show source-provider-aware save actions and version history for code-mode agent overrides.
