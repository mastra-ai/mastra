---
'@mastra/playground-ui': patch
---

Added always-visible version selector to the experiment trigger dialog for agent targets. Enabled for stored agents with version list, disabled with tooltip for code-defined agents. Gated behind CMS availability.
