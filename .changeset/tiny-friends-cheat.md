---
'mastra': patch
---

Added `--custom-args` flag to `mastra start`, matching the existing support in `mastra dev`. This lets you pass Node.js flags like `--require=newrelic` to the production server process.
