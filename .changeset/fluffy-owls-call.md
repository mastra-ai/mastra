---
'mastracode': minor
---

Added more ways to start and use goals.

Plans can now be accepted as goals directly from the inline plan approval UI. Slash commands can opt into goal mode with `goal: true`, and skills can opt into goal mode with `metadata.goal: true`.

`/goal` also now supports objectives that span multiple lines.
