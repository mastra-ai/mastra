---
'@mastra/factory': patch
---

Preserved every GitHub issue assignee end-to-end so Factory boards no longer drop co-assignees, included auto-triaged GitHub issues when building teammate options for the board filter dropdown, and backfilled missing assignee and reviewer metadata so the pull request reconciler stops re-fetching cards on every sweep.
