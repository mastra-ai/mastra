---
'@mastra/playground': patch
---

Replaced the hover-only feedback button in the advanced thread view with a Spans / Feedback tab list on each trace panel, matching the traces page. The Feedback tab shows a dot when some feedback still needs review, and its thread renders inline in the panel instead of a floating overlay.
Each trace panel also gets a "Go to trace" link that opens the same trace on the traces page.
