---
'@mastra/cloudflare': minor
---

Added type mappings for dataset and experiment tables (`mastra_datasets`, `mastra_dataset_items`, `mastra_dataset_versions`, `mastra_experiments`, `mastra_experiment_results`).

**Requires `@mastra/core` >= 1.4.0**
