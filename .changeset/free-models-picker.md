---
'mastracode': minor
---

OpenCode Zen now appears as a provider during onboarding and pack switching even without any API key: the /models picker marks Zen free models with a (free) label instead of showing them as missing a key, selecting them no longer prompts for an API key, and a built-in OpenCode Zen mode pack (plus OM pack) makes the free tier usable right after first launch.
