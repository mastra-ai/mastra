---
'@mastra/factory': patch
---

Fixed the board's Intake column loading every open pull request or issue of the repository while a search, teammate, or label filter was active.

Filters apply to the candidates already loaded, so a filtered Intake column now pages with the Load more button instead of pulling page after page behind a spinner.
