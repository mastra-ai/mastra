---
'@mastra/factory': patch
---

The transcript draws its tool cards, tool groups and file diffs from the shared design-system components. Diff lines are colored by the same highlighter as code blocks, so their colors now match the rest of the UI.
