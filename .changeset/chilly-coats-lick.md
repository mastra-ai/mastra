---
'@mastra/playground-ui': patch
---

Aligned component spacing and icon sizes to the design system scale. Buttons, inputs, menus, tooltips, workflow cards and list rows now use standard spacing steps and icon size tokens instead of one-off pixel and em values, so sizes stay consistent across components. Most elements shift by 1 to 2 pixels.
