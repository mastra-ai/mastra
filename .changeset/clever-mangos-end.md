---
'@mastra/factory': patch
---

Added unread attention for parked user sessions that are not bound to a work item, so they appear in the inbox and can mark User Sessions in the sidebar. Such a park is listed only for a viewer its session row admits — the same org-visible-plus-own-private rule the sessions list enforces.
