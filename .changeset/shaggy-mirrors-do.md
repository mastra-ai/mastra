---
'@mastra/behaviors': minor
---

Added the @mastra/behaviors package for defining durable, path-addressed agent behavior trees with mutable TypeScript resolvers or filesystem `BEHAVIOR.md` nodes. Includes transactional in-memory and LibSQL stores, guarded and judged navigation, safe handling for removed active nodes, and state-signal projection.
