---
'mastra': patch
---

Added shared-history conversation branching to Studio. Each message now carries a "Branch from here" action that forks the conversation at that point into a new thread sharing history up to the fork; both the source and branch views render an inline marker at the fork point with links between parent and branch, and the memory sidebar lists a thread's branches with links to each.
