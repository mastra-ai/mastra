---
'@mastra/server': patch
---

Bumped `@mastra/core` peer dependency floor to `>=1.35.0-0` so the widened FGA permission types and `MASTRA_USER_*` request-context constants are available. Older `@mastra/core` versions don't ship these.
