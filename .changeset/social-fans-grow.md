---
'@mastra/libsql': minor
'@mastra/pg': minor
---

Added `requestContext` and `requestContextSchema` column support to dataset storage. Dataset items now persist request context alongside input and ground truth data.
