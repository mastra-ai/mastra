---
'@mastra/playground-ui': patch
---

Fixed shared chat approval and answer controls to disable continuation actions after a run is blocked, and added chat context for execution eligibility and trusted run version identity. Dialogs and alert dialogs now honor reduced-motion preferences.
