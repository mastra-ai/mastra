---
'@mastra/core': patch
---

Added a `getCavemanObservations()` getter on `Harness` and per-thread persistence of the `cavemanObservations` thread-setting key, mirroring the existing observation/reflection threshold flow.
