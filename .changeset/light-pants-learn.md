---
'@mastra/ai-sdk': patch
---

Fixed direct routing responses showing internal routing reasoning before the final answer. Users now see only the final response when no delegation is needed. Fixes #12545.
