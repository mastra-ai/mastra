---
'@mastra/core': minor
---

Added `avatarUpload` feature flag to Agent Builder features. When enabled, agent owners can upload a custom avatar image that is downscaled to 256×256 and stored as a data URL in agent metadata.
