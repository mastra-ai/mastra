---
'@mastra/playground': patch
---

Added keyboard shortcuts in Studio: press `[` to collapse or expand the main sidebar, and press `{` on an agent chat page to hide or show the threads panel. Both are ignored while typing in an input. The matching buttons now show the key in their tooltip.
