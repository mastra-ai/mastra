---
'mastracode': minor
---

The /plugins Configure screen now supports plugin action options and conditional options. Plugins can define callback-type config options (for example an "Authenticate" action that runs a browser OAuth flow) and hide or show options and tools based on current config, so a plugin can show "Authenticate" when disconnected and "Disconnect" once connected.
