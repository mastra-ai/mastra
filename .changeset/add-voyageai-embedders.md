---
"@mastra/voyageai": minor
---

feat(voyageai): add VoyageAI embeddings and reranker integration

Adds `@mastra/voyageai` package with text embeddings, multimodal embeddings,
contextualized chunk embeddings, and rerankers using VoyageAI SDK 0.2.x.
Includes token-aware batching via SDK `tokenize()` for automatic input splitting.
