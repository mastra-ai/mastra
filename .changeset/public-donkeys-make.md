---
'@mastra/code-sdk': patch
'mastracode': patch
---

Added the Stagehand model to the /browser setup summary and to /browser status (also available as /browser info), including when Mastra Code silently falls back to your OpenAI Codex login, so you can always see which model drives browser actions.
