---
'mastra': patch
---

Added "go to" keyboard shortcuts in Studio: press `g` then `a` to open Agents, `g` then `w` to open Workflows, and `g` then `t` to open Traces. Inside an agent page, `g` then `t` opens that agent's traces instead of the global traces page.
