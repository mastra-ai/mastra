---
'@mastra/playground-ui': patch
---

Replaced EntryList component with Table component for scores and traces lists. This consolidates the UI to use a single table primitive with inline skeleton, empty state, pagination, and infinite scroll patterns.
