---
'@mastra/core': minor
---

Added `formatOutboundText` and `consumeStream` hooks to `channels` to customize per-chunk text formatting or fully replace streaming rendering. The default renderer is exported as `defaultConsumeStream` for wrapping.
