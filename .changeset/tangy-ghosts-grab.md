---
'create-factory': minor
---

Added CreateOS as an explicit sandbox provider in generated Factory projects. Set FACTORY_SANDBOX_PROVIDER=createos and provide a CreateOS API key to use it.

```bash
FACTORY_SANDBOX_PROVIDER=createos
CREATEOS_SANDBOX_API_KEY=your-api-key
```
