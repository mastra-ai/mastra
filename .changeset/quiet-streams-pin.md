---
'@mastra/deployer': patch
---

Exposed the resolved agent version continuation header through the default CORS configuration so cross-origin browser clients keep recursive legacy stream turns on immutable version pins.
