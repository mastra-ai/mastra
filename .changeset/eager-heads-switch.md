---
'@mastra/playground-ui': patch
'@mastra/factory': patch
---

Streamed replies now arrive with one motion. Tool rows, tool groups, cards and the thinking line fade in on the same curve as the words around them instead of popping in with a slide, and reasoning reveals word by word like the reply it belongs to. Rows already on screen when a session is restored no longer replay their entrance.
