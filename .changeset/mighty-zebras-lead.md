---
'@mastra/playground-ui': minor
---

Added dataset experiments comparison view with side-by-side experiment info, per-scorer summary table, per-item score deltas, and a detail panel for inspecting individual comparison items. Enhanced Chip component with new `intensity` prop (`bright` | `muted`), additional color options (purple, yellow, cyan, pink), and a new `ChipsGroup` component for visually connected chip sets. Improved ItemList spacing and cell alignment.
