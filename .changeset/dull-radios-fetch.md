---
'@mastra/playground-ui': patch
'@mastra/factory': patch
---

The markdown entrance animation is now a shared `mastra-arriving` class in `theme.css`, so any surface can land a block on the same curve as a streamed word. Renamed from `mastra-markdown-arriving`.
