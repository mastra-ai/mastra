---
'mastracode': minor
---

Added `/model` to change the model for the current mode. Changes persist across sessions, and changing a built-in pack creates the `Custom` pack. `/models` switches model packs, with `/packs` available as an alias.

```text
/model
/packs
```
