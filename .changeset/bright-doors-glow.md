---
'@mastra/playground-ui': patch
---

Replaced experiment dialog selectors with Combobox components for visual consistency. Target type and target ID now use `Combobox`, scorers use `MultiCombobox`. All three dropdowns share the same searchable dropdown style.
