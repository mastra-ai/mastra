---
'@mastra/playground': patch
---

Fix the scorer detail page layout: the scores list now sizes to its content like other list pages, and the loading skeleton keeps the same spacing as the loaded list.
