---
'@mastra/factory': minor
---

Added a `/web/workspace/plan` route that reads a plan Markdown file from the project's sandbox by path, so the plan-approval UI can render the plan's contents instead of showing a bare file path.
