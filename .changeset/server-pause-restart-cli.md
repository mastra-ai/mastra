---
'mastra': patch
---

Added `mastra server pause` and `mastra server restart` for Mastra Server projects. Pause stops the running server for your linked project; restart starts a paused or stopped server.
