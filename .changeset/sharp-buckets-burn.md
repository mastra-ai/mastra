---
'@mastra/memory': patch
---

Count image-like file parts with image token estimates in observational memory so uploaded images that arrive as `file` parts still trigger observation at the right thresholds.
