---
'@internal/playground': patch
---

Added the Agent Builder create page (`AgentBuilderStarter` component, `/agent-builder/agents/create` route file, and the redirect-only `AgentBuilderRoot` index). The route is not yet wired into the router; this is the first slice of a stacked PR series that will land the rest of the Agent Builder UI incrementally.
