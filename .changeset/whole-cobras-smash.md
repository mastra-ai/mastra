---
'@mastra/factory': patch
---

Fixed the model pickers in Settings stopping short of the right edge: on Memory and Models the observer/reflector and default-model dropdowns were sized to their text instead of filling their field, so they no longer lined up with the number inputs and controls in the rows below.
