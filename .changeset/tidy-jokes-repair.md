---
'@mastra/factory': patch
---

Added incident.io intake for self-managed servers: the generated Factory Server now wires the incident.io integration from INCIDENT_IO_API_KEY, and the Work Intake settings show incident.io source selection and routing when a deployment API key is configured, not only for Platform-managed connections.
