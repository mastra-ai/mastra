---
'@mastra/otel-exporter': minor
---

Add logs and metrics export support to @mastra/otel-exporter (logs path complete; metrics records every observation as an OTEL Histogram pending a follow-up rewrite to use the proper instrument per metric).
