---
'@mastra/playground-ui': patch
'mastra': patch
---

Traces: message actions in the trace thread (copy, "Highlight spans") only appear on hover, the span header no longer has a divider, and the Spans/Timeline/Feedback/Scores tab contents share one horizontal padding with the tab bar.
