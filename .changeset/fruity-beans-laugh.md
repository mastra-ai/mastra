---
'@mastra/gcs': minor
'@mastra/s3': minor
---

Updated minimum `@mastra/core` peer dependency to support the new `FilesystemLifecycle` and `SandboxLifecycle` interfaces and `onInit` / `onDestroy` lifecycle callbacks.

See the `@mastra/s3` and `@mastra/gcs` patch changesets for usage examples.
