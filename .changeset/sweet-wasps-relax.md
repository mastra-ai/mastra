---
'@mastra/client-js': minor
'@mastra/server': patch
'@mastra/core': patch
---

**Added** optional `author` field on `StoredAgentResponse` so consumers can render the resolved author (name, email, avatar) returned by stored-agent list and detail endpoints.
