---
'@mastra/client-js': minor
'@mastra/playground-ui': minor
---

Added typed nested metadata discovery responses and Studio metadata field selection. Selected string, number, and boolean values retain their types when saved in trace filter URLs.
