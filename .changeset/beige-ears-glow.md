---
'@mastra/code-sdk': major
---

Renamed the createMastraCode vector store option from `vectorStore` to `vector`.

Before:

```ts
createMastraCode({ vectorStore });
```

After:

```ts
createMastraCode({ vector });
```
