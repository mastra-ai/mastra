---
'@mastra/client-js': minor
---

agentController.session().create() accepts optional modeId/modelId creation seeds and returns the session's actual mode and model; listModes() entries now carry default and defaultModelId.
