---
'@mastra/playground-ui': minor
---

Added EditScorerForm UI for creating and editing stored scorer definitions in the CMS, with support for all 12 scorer types (llm-judge + 11 presets), conditional model/instructions fields, score range configuration, and default sampling settings.
