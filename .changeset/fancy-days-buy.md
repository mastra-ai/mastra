---
'@mastra/playground-ui': patch
---

Improved the datasets experience in Studio: creating and editing a dataset now happens on dedicated pages with proper breadcrumbs instead of dialogs, the dataset breadcrumb links to the dataset while a separate arrow opens the dataset switcher, item comparison moved to a path-based URL and is started from a new "Compare with" section in the item side panel, item checkboxes are always visible with contextual actions appearing once items are selected (replacing the "Select &" menu), and the "Run Experiment" button keeps a stable label.
