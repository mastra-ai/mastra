---
'@mastra/server': minor
---

Add authenticated Controller Session browser viewing and input routes. Resolve only an existing session, verify its resource, thread and browser launch, and bound queued frames for slow viewers.
