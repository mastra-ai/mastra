---
'mastra': minor
'@mastra/deployer': patch
---

Add --studio flag to bundle playground UI with mastra build

Enables bundling the studio/playground UI into the build output so it can be served from the deployed server.

```bash
mastra build --studio
```
