---
'@mastra/core': patch
---

Fixed processor API-error retries so prefill recovery reaches dedicated error processors again.

Also fixed a core typecheck regression in tool schema compatibility handling after merging main.
