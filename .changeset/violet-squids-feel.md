---
'@mastra/server': patch
---

Fixed thread and memory write authorization so new thread creation, cloning, saving messages, and resource-scoped search now respect Fine-Grained Authorization checks.
