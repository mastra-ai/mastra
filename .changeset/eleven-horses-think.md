---
'@mastra/memory': patch
'@mastra/core': patch
---

Improved observational memory continuation history so compact removed-context summaries now reuse the recall transcript format, and when retrieval is enabled the continuation message explains that higher-resolution details can be opened with the recall tool using the included message ids and part labels.
