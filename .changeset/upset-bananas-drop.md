---
'@mastra/code-sdk': patch
'mastracode': patch
---

Added resolveStagehandModel() so tools can report which model Stagehand will use and why (configured, Codex login fallback, or Stagehand default).
