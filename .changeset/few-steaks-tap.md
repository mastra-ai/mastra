---
'@mastra/playground-ui': patch
---

Trace Intelligence text now follows the design system text roles. Section labels, counts, dates and signal headers use the standard body font instead of monospace, headings no longer render a second page title, and the signals error state uses the standard error layout.
