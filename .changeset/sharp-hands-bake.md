---
'@mastra/core': minor
---

Added reusable validation for executable stored workflow definitions and fixed stored workflow publication so invalid or failed writes do not replace the live workflow.
