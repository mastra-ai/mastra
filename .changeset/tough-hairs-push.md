---
'@mastra/factory': patch
---

Improved factory review follow-up pull requests to satisfy repository automation: the reviewer now fills out the PR template, references the reviewed PR and its linked issue (without closing keywords), so triage bots no longer auto-close follow-up PRs for missing issue references.
