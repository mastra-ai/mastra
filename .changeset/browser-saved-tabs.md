---
'@mastra/agent-browser': minor
---

Add opt-in trusted input observation and inactivity closure for remote shared browsers. Save web page URLs and the selected tab through native thread storage and restore them on relaunch. Forms and login state are not saved. Long browser operations remain protected from idle closure.
