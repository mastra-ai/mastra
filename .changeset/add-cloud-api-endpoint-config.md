---
"@mastra/deployer": patch
---

Add support for configuring a cloud API endpoint via `MASTRA_CLOUD_API_ENDPOINT` environment variable. This value is now injected into the playground frontend as `window.MASTRA_CLOUD_API_ENDPOINT`.
