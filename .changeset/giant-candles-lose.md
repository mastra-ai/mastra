---
'@mastra/playground-ui': patch
---

Traces: "Score trace" is now a primary action in the trace header (tooltip shows the `s` shortcut) instead of an item in the actions menu; trace and span headers show their metadata inline next to the title
