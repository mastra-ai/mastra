---
'@mastra/playground-ui': patch
---

Improved the Checkbox component by migrating it to Base UI. The public API is unchanged — `checked`, `defaultChecked` (including the `'indeterminate'` value), `onCheckedChange`, and `disabled` all behave as before.
