---
'@mastra/playground-ui': patch
---

Migrated the Checkbox component to Base UI. The public API is unchanged — `checked`, `defaultChecked` (including the `'indeterminate'` value), `onCheckedChange`, and `disabled` all behave as before.
