---
'@mastra/playground-ui': patch
---

Improved workflow graph pages so the graph uses the full available width while the workflow details panel floats above it, the timeline aligns with the left panel width, and selected run headers show status, duration, and relative age metadata.
