---
'mastracode': patch
---

Replaced the MastraCode web studio's bespoke inline-SVG icon set with lucide-react icons and removed the decorative brand wordmark/logo, including the absolutely-positioned "MASTRA CODE" title. The UI now relies on the shared icon convention used across the design system while preserving all accessible labels.
