---
'@mastra/deployer': patch
---

Fixed deployed servers to preserve structured HTTP error responses and expose resolved-version metadata through the default CORS configuration. Browser SDKs can now keep legacy client-tool continuations pinned to the versions selected for their original run.
