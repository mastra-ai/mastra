---
'@mastra/playground': patch
---

Redesigned the Studio "Run experiment" dialog as a 640px single-column flow: a Dataset → Target → Scorers step timeline, a collapsed request-context disclosure, a footer status that lists missing fields, and Cmd/Ctrl+Enter to submit. Removed the target type picker from dataset create/edit forms and the Target filter from the datasets list; the Target column still shows types derived from experiments.
