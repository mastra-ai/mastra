---
'@mastra/core': minor
---

Added `createClassifierScorer()` to adapt one configured Classifier question into a Mastra scorer while preserving typed answers, probabilities, usage, warnings, and safe response metadata.
