---
'@mastra/core': minor
---

Added A2A v1.0 remote subagent delegation with explicit protocol selection on A2AAgent. Set `protocolVersion: '1.0'` to connect to v1.0-only agents while existing integrations continue to use v0.3 by default.
