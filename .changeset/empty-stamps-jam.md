---
'@mastra/playground-ui': patch
---

Design system text now uses the text roles without extra weight, line-height, or letter-spacing overrides. Table and list column headers use the column role, IDs and log lines render in monospace through Txt, tool names in approval prompts use InlineCode, and stacked layouts use gap instead of space utilities.
