---
'@internal/playground': patch
---

Workflow runs panel lists embedded-agent transcripts in chronological order (created time).

Agent sidebar hides workflow `createStep(agent)` threads by default; use **Show chats from workflow runs** under New Chat to include them (saved per browser).
