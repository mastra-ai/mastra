---
'@mastra/editor': patch
---

Improved the Agent Builder so it now defines a clear name, user-facing description, and complete system prompt for the agent before selecting capabilities. The builder no longer asks the user to write these fields and instead creates them based on the user's request.
