---
'mastra': patch
---

Traces: scoring a trace now opens a scorer dialog from the "Score trace" header action (also on `s`); the scorer picker is gone from the Scores tab and the span panel no longer has a Feedback tab
