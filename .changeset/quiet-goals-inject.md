---
'@mastra/core': patch
---

Thread `jsonPromptInjection` through the default goal scorer so goal judges can use prompt-injected structured output with providers that reject native response formats.
