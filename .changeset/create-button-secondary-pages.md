---
'@mastra/playground': patch
---

Added a New scorer action to the Scorers page header when the CMS is available, and made the create actions on the dataset detail page and the agent Evaluate tab consistent with the other create actions in Studio (shared style, tooltip, `C` shortcut).
