---
'@mastra/core': patch
---

Improved Harness v1 session reliability so callers get isolated state snapshots, accurate token totals when providers omit totals, and safer concurrent message handling during active runs.
