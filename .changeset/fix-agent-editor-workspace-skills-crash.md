---
'mastra': patch
---

Fixed a crash in the Agent Editor when opening a code-defined agent whose `Workspace` discovers skills. The editor now loads cleanly for these agents instead of showing the default error screen.
