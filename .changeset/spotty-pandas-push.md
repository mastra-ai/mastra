---
'@mastra/playground-ui': minor
---

Added a `search` flag to FilterBar fields: the field stays pinned at the top of the field list whatever is typed, so plain text commits as a filter on that field instead of forcing a field pick first. Options also take a `start` node (avatar, icon) rendered before their label, and the value list now opens on its first option after a field is picked with the keyboard.
