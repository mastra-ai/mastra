---
'@mastra/core': patch
---

Added DeepSeek V4.1 Flash to the DeepSeek provider catalog. Reference it as `deepseek/deepseek-flash` so it appears in the picker and keeps temperature, structured output, and image attachments enabled even when models.dev has not listed it yet.
