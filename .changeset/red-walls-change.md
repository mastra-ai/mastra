---
'@mastra/core': minor
'@mastra/deployer': patch
---

Added the `bundler.entries` config option, a map of output name to source path for extra process entries that `mastra build` should emit alongside the server bundle. See the `@mastra/deployer` changelog for details.
