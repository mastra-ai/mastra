---
'mastracode': patch
---

Improved the web chat connection so it keeps the event stream stable during chat updates, recovers more reliably after connection drops, and no longer flashes "Connection lost" while first connecting after a page refresh.
