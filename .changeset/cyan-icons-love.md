---
'@mastra/factory': minor
'@mastra/code-sdk': patch
'@mastra/core': patch
---

Added adjustable Build, Plan, and Fast thinking levels to custom model packs. In Factory settings, choose a thinking level for each model slot or select **Use default**; pack summaries display configured values such as `high thinking` next to the assigned model.
