---
'create-factory': patch
---

Fixed Factory project creation to use the scaffold bundled with the Mastra CLI instead of a remote template, preventing generated dependencies from drifting to unintended prerelease versions.
