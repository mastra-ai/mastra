---
'@mastra/playground': patch
---

Added a "Thread traces" link next to the Enriched toggle in an agent chat. It opens the agent's Traces tab with the thread id filter already applied, so the traces behind the conversation no longer have to be found by typing the thread id into the filter by hand.
