---
'@mastra/factory': patch
---

Show the initiating prompt, running tools, buffered output, and pending approvals when opening a thread during an automatically started run. Reconnects reconcile the current session with history, while local sends stay busy until the server starts processing them.
