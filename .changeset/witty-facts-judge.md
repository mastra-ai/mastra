---
'@internal/playground': patch
---

Improved Studio agent loading states and Memory sidebar polish. Agent pages now keep the redesigned shell visible while data loads, thread skeletons match embedded sidebar chrome, the Memory card better matches the surrounding sidebar, Memory copy/edit controls have cleaner keyboard and accessibility behavior, and the app shell no longer emits a React `forwardRef` warning on load.
