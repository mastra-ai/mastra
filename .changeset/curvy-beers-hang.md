---
'@mastra/playground-ui': patch
---

Fixed the Clear action in multi-select Combobox popups so the hover highlight moves smoothly onto it like any other row, instead of switching to a separate red button.
