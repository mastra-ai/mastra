---
'mastracode': patch
---

Improved MastraCode web syntax highlighting, switched the web UI to the shared Playground UI stylesheet, restored React Query-backed sidebar auth actions, and reorganized the web UI internals into reusable UI and domain folders.
