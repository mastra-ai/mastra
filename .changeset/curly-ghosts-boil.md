---
'@mastra/mcp': minor
'mastra': patch
---

Added `generateToolTypes()` and `MCPClient.generateTypeDeclarations()` for generating TypeScript type declarations from discovered MCP tool schemas (#21989)
