---
'mastra': patch
---

In step-by-step (debug) mode, the workflow graph now automatically pans and zooms to center the step the paused run is waiting on, so the step about to run is always in view. Clicking a different step in the timeline still takes priority and focuses that step instead.
