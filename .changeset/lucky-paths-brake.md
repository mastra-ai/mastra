---
'mastra': minor
---

Added the `mastra traces import --provider langfuse` command for dry-running, starting, and resuming 30-day trace migrations into Mastra Platform, including bounded local staging and upload-only resume.

```bash
mastra traces import --provider langfuse --dry-run
```
