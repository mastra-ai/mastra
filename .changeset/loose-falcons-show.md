---
'@mastra/deployer-sandbox': minor
---

Add `deployWorkerToSandbox` for running trusted non-HTTP artifacts with caller-owned execution IDs, bounded stdin or staged-file input, separate byte-preserving stdout/stderr reads, typed lifecycle outcomes, process-group cancellation, destroy retries, and serialized dependency-cache reuse.
