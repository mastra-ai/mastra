---
'@mastra/playground-ui': patch
---

Added `MainCard`, the rounded, raised surface that sits inside `AppShell` and holds the page content. Import it alongside `AppShell` from `@mastra/playground-ui/new/layout/app-shell`.
