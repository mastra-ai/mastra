---
'@mastra/core': minor
---

Added optional per-webhook agent override on AgentChannels.handleWebhookEvent for multi-tenant channel RBAC. Override is persisted on the Mastra thread so tool approvals use the same agent.
