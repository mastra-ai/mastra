---
'@mastra/factory': patch
---

Organization admins can now change deployment thinking defaults (base level and per-mode defaults) from Settings when authentication is enabled. Other members still see these settings as read-only, and signed-out requests to save them are rejected.
