---
'@mastra/playground-ui': patch
---

Kept trace metadata discovery compatible with structured Core responses while full typed metadata filtering is developed separately. Studio continues to expose the string metadata paths and suggestions it already supports, and ignores exact segment-array paths and non-string suggestions.
