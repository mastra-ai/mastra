---
'create-softwarefactory': patch
---

Added the create-softwarefactory CLI. Run npm create softwarefactory to scaffold a Mastra Software Factory project: pick a model provider, set up the Postgres database, and optionally connect WorkOS sign-in, a GitHub App (guided manual entry), and Linear. The CLI writes a ready-to-run .env and prints the dev URLs when it finishes.
