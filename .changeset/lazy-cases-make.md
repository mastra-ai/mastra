---
'@mastra/playground': patch
---

Fixed infinite loop on scorer detail page that caused the browser to hang when clicking a score row. The URL and component state now sync without fighting each other.
