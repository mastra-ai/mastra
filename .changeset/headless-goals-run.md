---
'mastracode': minor
---

Expose a headless `runGoal` API that starts goals through the same GoalManager and system-reminder signal path used by the TUI, resolving on terminal `goal_evaluation` events without manual continuation messages.
