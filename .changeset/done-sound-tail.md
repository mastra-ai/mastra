---
'@mastra/factory': patch
---

The completion chime is a new sound: a low swelling two-note figure over a short sub-bass body, ringing out into a 2.2 s convolution tail. It is still synthesized with the Web Audio API, so nothing is added to the bundle — the tail is a seeded, peak-normalized impulse response rather than an audio file.
