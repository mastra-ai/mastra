---
'mastra': minor
---

Added `mastra api factory` commands for projects, work items, decisions, attention, queue health, metrics, and supervisors.

```bash
mastra api factory project list
mastra api factory work-item transition --schema
```
