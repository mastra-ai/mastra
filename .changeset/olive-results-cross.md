---
'mastra': minor
---

Added `agentBuilder` flag to `create()` and `init()` to scaffold projects with Agent Builder pre-configured. When enabled, the CLI writes an index file with MastraEditor, createBuilderAgent, LibSQLStore, and Observability, and installs @mastra/editor alongside core dependencies.
