---
'@mastra/playground-ui': patch
---

Refined the focus state of single-line inputs (`Input`, `InputGroup`, `Searchbar`) in `@mastra/playground-ui`. Replaced the green focus border + ring with a subtle, opacity-based surface lift that works on any underlying background, and rounded the inputs fully to better follow the design system.
