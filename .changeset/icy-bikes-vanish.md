---
'mastra': patch
---

Fixed 'Reading environment variables...' message showing before the user selects an env file during deployment. The spinner no longer wraps the interactive env file selection prompt.
