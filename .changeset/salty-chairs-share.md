---
'@mastra/playground-ui': patch
---

Added yaml, diff, css, html, xml and sql to the languages highlighted in code blocks, so markdown fences and chat output in those languages are no longer rendered as plain text.
