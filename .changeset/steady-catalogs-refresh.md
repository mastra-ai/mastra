---
'mastra': patch
'@mastra/mcp': patch
---

Expose the Factory `work-item automation-run` route through `mastra api`, and refresh the generated Mastra API MCP tool catalog so it includes `trace_query`, `score_delete`, and `feedback_delete`.
