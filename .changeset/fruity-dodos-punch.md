---
'@mastra/factory': patch
---

The work item comment feed now renders through the shared design-system `Comment` component, so comment rows, quoted replies and inline editing look and behave the same anywhere they appear. Row actions (quote, copy link, edit, delete) now appear when you hover that row rather than anywhere on the card.
