---
'@mastra/agent-browser': minor
---

Add responsive viewport and language preferences to the existing browser. Configure `viewerPreferences: { width: 1280, height: 720, deviceScaleFactor: 2, locale: 'en-US' }` and a matching screencast size for sharp frames. Resizing preserves pages and unsaved input.
