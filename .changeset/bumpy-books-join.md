---
'@mastra/code-sdk': patch
---

Fixed plugin loading to reject separate Mastra Code runtimes before executing plugin code, with actionable dependency-alignment guidance instead of storage initialization errors.
