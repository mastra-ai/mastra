---
"@mastra/core": minor
"@mastra/cli": minor
---

Implemented Skills feature: reusable instruction sets for agents that can be loaded dynamically.
- Added `skills` module to `@mastra/core`
- Added `skills` configuration to `Agent`
- Added CLI commands: `mastra skill create`, `mastra skill validate`, `mastra skill list`
