# Family Fit Matrix

| Family | Source | Surface | Primitive Fit | Suggested Type | Suggested Action | Shape Notes | Verdict |
| --- | --- | --- | --- | --- | --- | --- | --- |
| Inbound channel message | `channels/agent-channels.ts`, `agent-controller/session.ts` | channel/session | Pulse | `input` | `introduced` | Owns new user content/files. Relationships connect external message id, thread id, and eventual model input. | Apply |
| Channel history block | `channels/agent-channels.ts` | channel/content | Pulse if preserved as separate content | `input` | `introduced` | Today history is flattened into the message text. Initial Pulse can represent the resulting content; separate platform-history reconstruction is deferred. | Defer |
| Agent signal routing | `agent-controller/session.ts`, Agent Signal runtime | agent/session | Pulse + Relationship | `decision` | `delivery_decided` | Reuse prior Agent Signal model. Delivery is not model visibility. | Apply |
| Follow-up queue | `agent-controller/session.ts` | session/run-control | ChangePulse + Relationship | `state` | `queued`, `drained`, `requeued` | User messages submitted mid-run are queued and later sent or requeued on failure. This overlaps signal queue reconstruction but is session-owned. | Apply selectively |
| Model visibility | `session-run-engine.ts`, MessageList/stream runtime | model/session | Pulse + Relationship | `input` | `assembled` | `included_in_model_input` is the reconstruction boundary. | Apply |
| Tool approval | `agent-controller/session.ts`, channels | tool/session | Pulse or ChangePulse | `decision` | `approval_required`, `approved`, `declined` | Approval policy changes are ChangePulses; individual decisions are Pulses. | Apply |
| Stale channel approval | `agent-controller-channels.ts` | channel/tool | Pulse | `decision` | `stale_approval_ignored` | User clicked an approval for a gate no longer armed after restart. Core refuses execution; durable host may settle the attempt. | Apply selectively |
| Tool suspension/resume | `agent-controller/session.ts`, stream runtime | tool/session | Pulse + Relationship | `state` | `suspended`, `resumed` | Connect resume to suspended tool/run with purpose-named relationships. | Apply |
| Mode/model/session setting | `agent-controller/session.ts` | agent_config/session | ChangePulse | `state` | `changed` | Mode/model/subagent model and permission grants are behavior-changing session/thread settings. | Config provenance |
| Display state | `agent-controller/session.ts` | UI/session | none | n/a | n/a | Derived UI mirror unless it is the only durable record of user-visible state. | Skip |
| Background task lifecycle | `background-tasks/manager.ts`, `background-tasks/workflow.ts` | tool/task | Pulse + Relationship | `state` | `enqueued`, `running`, `suspended`, `resumed`, `completed`, `failed`, `cancelled` | Apply only when task originates from agent/tool execution. Include `timed_out` as failed outcome/reason. | Apply selectively |
| Background task progress/output | `background-tasks/workflow.ts` | task/content | Pulse | `progress` or `output` | `emitted` | `task.output` chunks can be progress or content depending on payload and whether they re-enter stream/model context. | Apply selectively |
| Background task retry/recovery | `background-tasks/manager.ts`, `background-tasks/workflow.ts` | task/execution | ChangePulse or Pulse | `state` | `retry_scheduled`, `recovered`, `failed` | Apply when recovery changes the task outcome or retry count. Skip redelivery mechanics that do not change semantic state. | Apply selectively |
| Background task pubsub | `background-tasks/manager.ts` | infrastructure | none | n/a | n/a | Subscription and fan-out details are infrastructure. Delivery attempts only matter when they change retry/recovery semantics. | Skip by default |
| File-routed agent config | `agent/fs-routing/index.ts` | definition/config | Definition + ChangePulse | `state` | `resolved`, `changed` | Definitions are referenced; runtime records active applicability. | Config provenance |
| Skill resolution/injection | `skills/*`, `processors/processors/skills.ts` | definition/model-input | Definition + Relationship | `input` | `included` | Skill metadata becomes model-visible context; body should be referenced when reusable. | Apply at caller |
| Experiment run lifecycle | `datasets/experiment/index.ts`, `datasets/experiment/events.ts` | eval/runtime | Pulse | `state` | `started`, `finished`, `failed` | Existing observer events are compact semantic facts. Do not duplicate storage status updates. | Apply selectively |
| Experiment hooks | `datasets/experiment/index.ts` | eval/runtime | Pulse | `state` | `hook_started`, `hook_completed`, `hook_failed` | Hooks affect setup/teardown and can fail/skips items. First spec can defer if experiment support is not initial. | Apply selectively |
| Experiment item/scorer | `datasets/experiment/*`, `evals/*` | eval/runtime | Pulse + Relationship | `state` | `completed`, `score_recorded` | Scorer output/score can be Pulse data, linked to item, scorer definition, target trace, and experiment. | Apply selectively |
| Dataset bookkeeping | storage/dataset APIs | storage/admin | none | n/a | n/a | CRUD/list/read does not explain runtime unless selected as experiment input. | Skip |
| Dynamic workflow run | `workflows/*` | workflow/runtime | Pulse + Relationship | `state` | `started`, `suspended`, `resumed`, `completed` | Existing core Pulse domain; definitions referenced by version. | Apply |
| Schedule-fired action | `schedules/worker.ts` | schedule/runtime | Pulse + Relationship | `input` or `state` | `fired`, `introduced`, `skipped`, `failed` | Apply when schedule causes content, signal, workflow, or run. Trigger row persistence is not itself the source fact. | Apply selectively |
| Provider capability table | `llm/model/provider-*` | definition | DefinitionReference | n/a | n/a | Static generated definitions; no runtime Pulse by itself. | Reference only |
| Concrete model route/fallback | model call sites/gateway, `session-run-engine.ts` fallback notice | model/runtime | Pulse + Relationship | `decision` | `selected`, `fallback_used` | Record selected model/provider/options/fallback when used. Info notice alone is too weak. | Apply |
