# Vector Store Source Management - Architecture Diagram

## Current Architecture (What Exists Today)

```
┌─────────────────────────────────────────────────────────────────┐
│                         User's Code                             │
├─────────────────────────────────────────────────────────────────┤
│  const chunks = await doc.chunk({ strategy: 'recursive' })      │
│  const { embeddings } = await embedMany({ ... })                │
│  await pgVector.upsert({                                        │
│    vectors: embeddings,                                         │
│    metadata: chunks.map(c => ({ text: c.text }))               │
│  })                                                             │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                    MastraVector (Base Class)                    │
├─────────────────────────────────────────────────────────────────┤
│  abstract query(params)                        ✅ Has filters   │
│  abstract upsert(params)                       ❌ No deleteFirst│
│  abstract deleteVector(params: { id })         ❌ Single ID only│
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                        PgVector Class                           │
├─────────────────────────────────────────────────────────────────┤
│  query({ filter })            ✅ Already supports rich filters  │
│  upsert({ vectors, metadata }) ❌ Can't delete old chunks       │
│  deleteVector({ id })          ❌ One at a time                 │
│                                                                 │
│  transformFilter(filter)      ✅ MongoDB-like operators         │
│  buildFilterQuery(filter)     ✅ SQL generation                 │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                    PostgreSQL Database                          │
├─────────────────────────────────────────────────────────────────┤
│  CREATE TABLE vectors (                                         │
│    vector_id TEXT PRIMARY KEY,                                  │
│    embedding vector(1536),                                      │
│    metadata JSONB          ✅ Can store ANY metadata            │
│  );                                                             │
│                                                                 │
│  Sample Data:                                                   │
│  vector_id: "abc123"                                            │
│  metadata: { "text": "...", "custom": "..." }                   │
│            ☝️ No source tracking!                                │
└─────────────────────────────────────────────────────────────────┘
```

## Proposed Architecture (Minimal Changes Needed)

```
┌─────────────────────────────────────────────────────────────────┐
│                         User's Code                             │
├─────────────────────────────────────────────────────────────────┤
│  const sourceMetadata = {                                       │
│    id: 'manual.pdf',                                            │
│    bucket: 'docs',                                              │
│    version: '2.0'                                               │
│  }                                                              │
│                                                                 │
│  const chunks = await doc.chunk(                                │
│    { strategy: 'recursive' },                                   │
│    sourceMetadata          🆕 Pass source info                   │
│  )                                                              │
│                                                                 │
│  await pgVector.upsert({                                        │
│    vectors: embeddings,                                         │
│    metadata: chunks.map(c => ({                                 │
│      text: c.text,                                              │
│      source_id: 'manual.pdf',  🆕 For filtering                  │
│      ...c.metadata                                              │
│    })),                                                         │
│    deleteFilter: { source_id: 'manual.pdf' }  🆕 Delete old!     │
│  })                                                             │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                    MastraVector (Base Class)                    │
├─────────────────────────────────────────────────────────────────┤
│  abstract query(params)                        ✅ Has filters   │
│  abstract upsert(params)                       🆕 + deleteFilter │
│  abstract deleteVector(params: { id })         ✅ Keep existing  │
│  abstract deleteVectorsByFilter(params)        🆕 New method     │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                        PgVector Class                           │
├─────────────────────────────────────────────────────────────────┤
│  query({ filter })                  ✅ No changes needed        │
│  upsert({ vectors, deleteFilter })  🆕 Delete first, then insert │
│  deleteVector({ id })               ✅ Keep for single deletes  │
│  deleteVectorsByFilter({ filter })  🆕 Batch delete by filter   │
│                                                                 │
│  Implementation:                                                │
│  ┌────────────────────────────────────────────────────────┐    │
│  │ async deleteVectorsByFilter({ filter }) {              │    │
│  │   const { sql, values } = buildFilterQuery(filter)     │    │
│  │   await query(`DELETE FROM ... WHERE ${sql}`, values)  │    │
│  │ }                                                      │    │
│  │                                                        │    │
│  │ async upsert({ vectors, metadata, deleteFilter }) {    │    │
│  │   BEGIN TRANSACTION                                    │    │
│  │   if (deleteFilter) {                                  │    │
│  │     await deleteVectorsByFilter({ filter: deleteFilter })│   │
│  │   }                                                    │    │
│  │   // ... insert vectors ...                           │    │
│  │   COMMIT                                               │    │
│  │ }                                                      │    │
│  └────────────────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                    PostgreSQL Database                          │
├─────────────────────────────────────────────────────────────────┤
│  CREATE TABLE vectors (                                         │
│    vector_id TEXT PRIMARY KEY,                                  │
│    embedding vector(1536),                                      │
│    metadata JSONB          ✅ Same schema!                       │
│  );                                                             │
│                                                                 │
│  Sample Data (After):                                           │
│  vector_id: "abc123"                                            │
│  metadata: {                                                    │
│    "text": "...",                                               │
│    "source_id": "manual.pdf",     🆕 For source tracking         │
│    "bucket": "docs",              🆕 For multi-tenancy           │
│    "indexed_at": "2025-01-01",    🆕 For temporal cleanup        │
│    "version": "2.0"               🆕 For versioning              │
│  }                                                              │
│                                                                 │
│  Recommended Indexes:                                           │
│  CREATE INDEX idx_source ON vectors                             │
│    USING GIN ((metadata->'source_id'));                         │
└─────────────────────────────────────────────────────────────────┘
```

## Flow Diagram: Updating a Document

### Old Way (Problem)

```
┌─────────────┐
│ Doc v1      │
│ "Hello..."  │
└─────────────┘
      │
      │ chunk()
      ▼
┌──────────────────────────────────────┐
│ Chunk 1      Chunk 2      Chunk 3   │
│ "Hello..."   "World..."   "Bye..."  │
└──────────────────────────────────────┘
      │
      │ embed()
      ▼
┌──────────────────────────────────────┐
│ Vector 1     Vector 2     Vector 3   │
│ id: "abc"    id: "def"    id: "ghi"  │
└──────────────────────────────────────┘
      │
      │ upsert()
      ▼
┌─────────────────────────────────────────┐
│           PostgreSQL                    │
│  ┌───────────────────────────────────┐  │
│  │ abc -> [0.1, 0.2, ...] {...}      │  │
│  │ def -> [0.3, 0.4, ...] {...}      │  │
│  │ ghi -> [0.5, 0.6, ...] {...}      │  │
│  └───────────────────────────────────┘  │
└─────────────────────────────────────────┘

--- Time passes, doc updated ---

┌─────────────┐
│ Doc v2      │
│ "Hi..."     │  (Shorter, only 2 chunks)
└─────────────┘
      │
      │ chunk()
      ▼
┌──────────────────────────┐
│ Chunk 1      Chunk 2     │
│ "Hi..."      "There..."  │
└──────────────────────────┘
      │
      │ embed()
      ▼
┌──────────────────────────┐
│ Vector 1     Vector 2    │
│ id: "jkl"    id: "mno"   │
└──────────────────────────┘
      │
      │ upsert()
      ▼
┌─────────────────────────────────────────┐
│           PostgreSQL                    │
│  ┌───────────────────────────────────┐  │
│  │ abc -> [0.1, 0.2, ...] {...}      │  │ ⚠️ STRANDED!
│  │ def -> [0.3, 0.4, ...] {...}      │  │ ⚠️ STRANDED!
│  │ ghi -> [0.5, 0.6, ...] {...}      │  │ ⚠️ STRANDED!
│  │ jkl -> [0.7, 0.8, ...] {...}      │  │ ✅ New
│  │ mno -> [0.9, 1.0, ...] {...}      │  │ ✅ New
│  └───────────────────────────────────┘  │
│                                         │
│  Problem: Old vectors (abc, def, ghi)  │
│  are still there but shouldn't be!     │
└─────────────────────────────────────────┘
```

### New Way (Solution)

```
┌─────────────┐
│ Doc v1      │
│ "Hello..."  │
└─────────────┘
      │
      │ chunk(config, { id: 'manual.pdf' })
      ▼
┌──────────────────────────────────────────────────────────┐
│ Chunk 1                Chunk 2                Chunk 3    │
│ text: "Hello..."       text: "World..."       text: "Bye"│
│ source_id: manual.pdf  source_id: manual.pdf  source...  │
└──────────────────────────────────────────────────────────┘
      │
      │ embed()
      ▼
┌──────────────────────────────────────┐
│ Vector 1     Vector 2     Vector 3   │
│ id: "abc"    id: "def"    id: "ghi"  │
└──────────────────────────────────────┘
      │
      │ upsert({
      │   metadata: [
      │     { text: "...", source_id: "manual.pdf" },
      │     { text: "...", source_id: "manual.pdf" },
      │     { text: "...", source_id: "manual.pdf" }
      │   ]
      │ })
      ▼
┌─────────────────────────────────────────────────────────┐
│           PostgreSQL                                    │
│  ┌───────────────────────────────────────────────────┐  │
│  │ abc -> [...] { source_id: "manual.pdf", ... }     │  │
│  │ def -> [...] { source_id: "manual.pdf", ... }     │  │
│  │ ghi -> [...] { source_id: "manual.pdf", ... }     │  │
│  └───────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────┘

--- Time passes, doc updated ---

┌─────────────┐
│ Doc v2      │
│ "Hi..."     │
└─────────────┘
      │
      │ chunk(config, { id: 'manual.pdf' })
      ▼
┌──────────────────────────────────────┐
│ Chunk 1              Chunk 2         │
│ text: "Hi..."        text: "There"   │
│ source_id: manual... source_id: ...  │
└──────────────────────────────────────┘
      │
      │ embed()
      ▼
┌──────────────────────────┐
│ Vector 1     Vector 2    │
│ id: "jkl"    id: "mno"   │
└──────────────────────────┘
      │
      │ upsert({
      │   metadata: [...with source_id...],
      │   deleteFilter: { source_id: "manual.pdf" }  🆕 Magic!
      │ })
      ▼
┌─────────────────────────────────────────────────────────┐
│           PostgreSQL                                    │
│                                                         │
│  Step 1: DELETE WHERE source_id = "manual.pdf"         │
│  ┌───────────────────────────────────────────────────┐  │
│  │ (empty - all old chunks deleted)                  │  │
│  └───────────────────────────────────────────────────┘  │
│                                                         │
│  Step 2: INSERT new chunks                             │
│  ┌───────────────────────────────────────────────────┐  │
│  │ jkl -> [...] { source_id: "manual.pdf", ... }     │  │
│  │ mno -> [...] { source_id: "manual.pdf", ... }     │  │
│  └───────────────────────────────────────────────────┘  │
│                                                         │
│  ✅ Clean state! No stranded vectors!                   │
└─────────────────────────────────────────────────────────┘
```

## Comparison: Proposal A vs B vs Our Solution

```
┌────────────────────────────────────────────────────────────────────┐
│                        Proposal A                                  │
│                   (Special source_id column)                       │
├────────────────────────────────────────────────────────────────────┤
│  Schema Change:                                                    │
│    ALTER TABLE vectors ADD COLUMN source_id TEXT;                  │
│                                                                    │
│  Pros:                                                             │
│    + Fast queries (indexed column)                                 │
│    + Clear separation                                              │
│                                                                    │
│  Cons:                                                             │
│    - Breaking schema change                                        │
│    - Only one dimension (what about tenant? bucket? version?)      │
│    - Need more columns for other use cases                         │
│    - Doesn't scale                                                 │
└────────────────────────────────────────────────────────────────────┘

┌────────────────────────────────────────────────────────────────────┐
│                        Proposal B                                  │
│                   (MDocument + metadata filters)                   │
├────────────────────────────────────────────────────────────────────┤
│  Approach:                                                         │
│    - Add MDocument metadata                                        │
│    - Filter-based operations                                       │
│    - No schema changes                                             │
│                                                                    │
│  Pros:                                                             │
│    + Flexible (any metadata)                                       │
│    + No schema changes                                             │
│    + Extensible                                                    │
│                                                                    │
│  Cons:                                                             │
│    - As described in issue, overly complex                         │
│    - Doesn't leverage existing Mastra infrastructure               │
└────────────────────────────────────────────────────────────────────┘

┌────────────────────────────────────────────────────────────────────┐
│                      OUR SOLUTION                                  │
│         (Metadata + existing filter system + minimal API)          │
├────────────────────────────────────────────────────────────────────┤
│  Approach:                                                         │
│    - Use existing metadata JSONB column                            │
│    - Leverage existing filter translation                          │
│    - Add deleteVectorsByFilter() method                            │
│    - Add optional deleteFilter to upsert()                         │
│    - Integrate with MDocument system                               │
│                                                                    │
│  Pros:                                                             │
│    + ✅ No schema changes                                           │
│    + ✅ Flexible (unlimited metadata fields)                        │
│    + ✅ Leverages existing infrastructure                           │
│    + ✅ Minimal code changes                                        │
│    + ✅ Works with existing filter system                           │
│    + ✅ Supports all use cases (source, tenant, bucket, version)    │
│    + ✅ Can add GIN indexes on specific fields if needed            │
│    + ✅ Consistent with Mastra patterns                             │
│                                                                    │
│  Cons:                                                             │
│    - Requires GIN indexes for best performance (easy to add)       │
│    - Not all vector stores support filtering (document it)         │
└────────────────────────────────────────────────────────────────────┘
```

## Code Size Comparison

```
┌─────────────────────────────────────────────────────────────┐
│  Lines of Code Needed                                       │
├─────────────────────────────────────────────────────────────┤
│  Proposal A (source_id column):                             │
│    - Schema migration:           ~50 lines                  │
│    - Add column to CREATE:       ~5 lines                   │
│    - Update all queries:         ~100 lines                 │
│    - Add delete by source:       ~30 lines                  │
│    - Tests:                      ~200 lines                 │
│    ─────────────────────────────────────                    │
│    Total:                        ~385 lines                 │
│                                                             │
│  Proposal B (as described):                                 │
│    - MDocument storage:          ~200 lines                 │
│    - Filter system from scratch: ~500 lines                 │
│    - Delete operations:          ~100 lines                 │
│    - Integration:                ~200 lines                 │
│    - Tests:                      ~300 lines                 │
│    ─────────────────────────────────────                    │
│    Total:                        ~1300 lines                │
│                                                             │
│  Our Solution:                                              │
│    - Add deleteVectorsByFilter:  ~50 lines                  │
│    - Update upsert signature:    ~5 lines                   │
│    - Add deleteFilter logic:     ~20 lines                  │
│    - MDocument integration:      ~50 lines                  │
│    - Helper functions:           ~80 lines                  │
│    - Tests:                      ~150 lines                 │
│    ─────────────────────────────────────                    │
│    Total:                        ~355 lines                 │
│                                                             │
│  Reuse existing infrastructure:  ~1500 lines                │
│    (filter translation, SQL builder, etc - already exists!) │
└─────────────────────────────────────────────────────────────┘
```

## Summary

**Key Insight**: Mastra already has powerful metadata filtering. Just expose it for deletions!

```
Current:  query() ──────→ Filter ✅
          deleteVector() → Single ID only ❌

Proposed: query() ──────→ Filter ✅
          deleteVectorsByFilter() → Filter ✅  🆕
          upsert({ deleteFilter }) → Filter ✅  🆕
```

This unlocks:

- ✅ Source-based management (the issue's request)
- ✅ Multi-tenancy
- ✅ Versioning
- ✅ Temporal cleanup
- ✅ Bucket-based organization
- ✅ Any custom metadata dimension
