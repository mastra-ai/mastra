# @mastra/opencode

`@mastra/opencode` will connect Mastra to the OpenCode SDK.

## Installation

```bash
npm install @mastra/opencode @opencode-ai/sdk
```

The agent wrapper API will be documented when its implementation is added.
