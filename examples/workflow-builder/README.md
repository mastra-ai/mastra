# Workflow Builder local review

This is an intentionally untracked local project for reviewing the persisted Workflow Builder in Studio.

```bash
pnpm install --ignore-workspace
pnpm dev
```

Set `OPENAI_API_KEY` before starting Studio. Open the Workflow Builder navigation item, create and save a workflow, revisit it, hand it off to the canonical workflow page, run it, and delete it.
