# At least one provider API key is required.
# The example uses Anthropic by default — set this:
ANTHROPIC_API_KEY=sk-ant-...

# Or switch to another provider in src/index.ts and set that key instead:
# OPENAI_API_KEY=sk-...
# GOOGLE_GENERATIVE_AI_API_KEY=...
