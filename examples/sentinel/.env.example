# Required — OpenAI API key for the agent model
OPENAI_API_KEY=sk-...

# Optional — SAM.gov API key for vendor registry lookups (free at sam.gov)
# SAM_GOV_API_KEY=...

# Database — local SQLite file for storage
DB_URL=file:local.db
