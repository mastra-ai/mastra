# Only the LLM-judge scorers and the Studio surface need this.
# Exercises 1 and 3-12 run without it (deterministic mock model) — that is
# exactly what `pnpm ex:offline` runs. Only exercise 2 needs a key.
OPENAI_API_KEY=sk-...
