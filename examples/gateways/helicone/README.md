# Helicone Gateway Example

Run a simple Agent via Helicone (OpenAI-compatible base URL).

## Setup

1. Install deps

```bash
pnpm i
```

2. Environment

```bash
cp .env.example .env
# edit .env and set HELICONE_API_KEY
```

## Run

```bash
pnpm start
```
